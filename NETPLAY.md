# Online 2-Player Co-op — Implementation Worklog

Working doc for the netplay effort. The full approved plan is reproduced in the appendix at the bottom of this file; the sections here are the operational summary.

## Decisions (locked with Glenn)

- **Transport**: WebRTC DataChannels. Native = **libdatachannel C API** (`rtc/rtc.h`; lib builds C++17 internally, game stays C++11). Web = browser RTCPeerConnection via EM_JS in `net_transport_web.cpp`.
- **Topology**: P2P host-authoritative. Host sims everything; client sends INPUT (unreliable channel), receives chunked snapshots (reliable channel, 10 Hz, 15 KB chunks) = savegame serialization + NetExtras (ship transients, projectiles, asteroid net_id array).
- **Signaling M1**: manual clipboard copy-paste of base64 SDP (non-trickle). No server, no PlayFab, no TURN. Room codes/signaling service = M2.
- **M1 platforms**: Windows (xbox/CMakeLists.txt GDK Desktop) + web. Other platforms compile the seam disabled (`NEWTONIA_NET_RTC` defined only by xbox CMake; web keys off `__EMSCRIPTEN__`; console smoke/Android/iOS get empty TUs).
- **Client sync**: snapshot-driven + local extrapolation (no lockstep — `rand()` is host-only authority). Asteroids get `uint32_t net_id` for cross-snapshot matching; local ship lerp-corrected (~0.35/snapshot); pickups rebuilt per snapshot.
- **Save protection**: all save paths (`save_progress`, death save, game-over delete) hard-gated off when `net_mode_ != Off`. Intro states suppressed online (2 s banner instead). `focus_lost` doesn't auto-pause online.
- **Pins**: MbedTLS `v3.6.6`, libdatachannel `v0.24.5` (verified to exist via ls-remote 2026-07-04).

## Phase status

- [x] **Phase 0 — spike**: prove FetchContent MbedTLS+libdatachannel, USE_MBEDTLS=ON, NO_MEDIA/NO_WEBSOCKET/NO_EXAMPLES/NO_TESTS, /MT static CRT, MSVC; link + run a C-API loopback (also validates `rtcDataChannelInit`/reliability struct). **SPIKE PASS in CI** (`spike/netplay/` + `netplay-spike.yml`, windows-latest, vcvars64+Ninja): both channels echo, `dumpbin /dependents` shows only WS2_32/bcrypt/KERNEL32 (no dynamic CRT), ~3.5 min runtime. Three CMake fixes were needed — all must be replicated in Phase 2's xbox/CMakeLists.txt block (see session log).
- [x] **Phase 1 — seam**: `net_transport.h/.cpp` (interface + factory→nullptr + `net_available()`), `net_transport_rtc.cpp` (all inside `#ifdef NEWTONIA_NET_RTC`), `net_transport_web.cpp` (all inside `#ifdef __EMSCRIPTEN__`), `net_protocol.h`. No behavior change; every build globs them fine. **Landed 955e357; all platform workflows green** (web.yml only runs on master/main — web stub syntax-checked locally with `-D__EMSCRIPTEN__`).
- [x] **Phase 2 — native backend**: NEWTONIA_NET option block in xbox/CMakeLists.txt (FetchContent per pins above, link `LibDataChannel::LibDataChannelStatic`); fill rtc backend (2 channels "rel"/"unrel", mutex inbound deque, atomics, non-trickle: expose SDP only at RTC_GATHERING_COMPLETE); `NEWTONIA_NET_SELFTEST=1` env → in-process loopback PASS/FAIL log in xbox_main.cpp. **Landed da835b3; NET SELFTEST PASS on Windows CI** (xbox-dev.yml now runs the loopback against the built exe every push — offer/answer/connected in ~100 ms each, both channels echo) **and on Linux** (local harness, same pins); all 7 workflows green.
- [x] **Phase 3 — web backend**: EM_JS `Module.__nwnet` (pc, rel, unrel, inbox[], state, localDesc), poll-style; `navigator.clipboard` helpers (gesture-gated read). **Landed 6c66a0f; WEB NET TEST PASS** — Playwright drove two pages of the real build through the C++ WebTransport path (handshake, bytes both ways on both channels, clipboard round-trip). Handle-based glue (multiple transports per page, testable from the console via `nwtest_*` exports). Non-trickle SDP with an 8 s snapshot fallback for browsers stuck in 'gathering'. Uniform `net_clipboard_*` helpers added for the Phase 5 lobby (sync SDL native / async web).
- [x] **Phase 4 — savegame streams**: `Save::Stream`/`FileStream`/`MemStream`; `wv/rv/wa/ra` + per-type helpers `FILE*`→`Stream&`; `serialize_game`/`deserialize_game` (no magic/version — save_game/load_game wrap; deserialize takes the file's version for the v10 mini-station gate). **Landed 510cf0b; byte-identical verified** (synthetic GameState covering every format branch → identical sha256 before/after; MemStream serialize→deserialize→serialize reproduces the file body exactly); all platform workflows green.
- [x] **Phase 5 — lobby + handshake**: `net_session.h/cpp` (HELLO/WELCOME/REJECT, chunk/reassemble, base64), `net_lobby.h/cpp` State (HOST/JOIN clipboard flow), ONLINE row in menu.cpp behind `net_available()`. Exit: two instances CONNECTED. **Landed beee201; exit criterion met on web** — Playwright drove two tabs purely through the lobby UI + browser clipboard to CONNECTED/PLAYER 1+2 (handshake Ready on both). Signal blobs are `NWTN1O.`/`NWTN1A.` + base64. Snapshot chunk/reassemble deferred to Phase 6 (nothing produces snapshots yet). Native↔native pending a second Windows box or cross-play test in Phase 8.
- [x] **Phase 6 — host side**: GLGame net ctor + `net_mode_`, `add_remote_player()` (add_player2 minus bindings), remote input via `net_host_poll()` (held bitmask + wrapping one-shot counters + respawn tap per glship.cpp:513), snapshot build/send @10 Hz, gating (saves/intro/focus/add_player2), single viewport online, asteroid `net_id`. **Landed b5c337b; verified end-to-end on web** (real host through the lobby + raw JS peer: WELCOME, 10 Hz chunk stream, INPUT thrust moved p2 ~1500 units in the host sim, remote car visibly thrusting in host screenshots). Also fixed two split-screen assumptions (`draw_map` divider/minimap placement, `setup_viewport` portrait flip) behind a new `split_screen()` helper — online has 2 players but 1 viewport. Pause sync + debug-key gating on client + dead-man switch remain Phase 8.
- [x] **Phase 7 — client side (the milestone)**: `tick_net_client()` (visual/kinematic stepping only), snapshot apply (net-id map, death debris for missing ids, generation rebuild via rollover-block clone, local-ship blend), INPUT send per tick, client bootstrap via existing `GLGame(const Save::GameState&,…)` from snapshot #1. **Landed fac5a77; TWO MACHINES PLAY CO-OP** — verified native↔native (two instances under Xvfb through the real lobby + X11 clipboard; both in-game, joiner input moves its ship across the host's world) AND web↔web (Playwright two tabs, same flow). Bonus: `make NETPLAY=1 NETPLAY_PREFIX=$(brew --prefix)` enables the native backend on macOS/Linux via installed libdatachannel (the Makefile-netplay stretch goal).
- [x] **Phase 8 — polish**: pause propagation, disconnect UX ("CONNECTION LOST"), generation banner, 1 s input dead-man switch, REJECT UX, native↔web cross-play checklist. **Landed 1a644cf** — pause syncs via EV_PAUSE/EV_RESUME (host polls before its pause gate so remote RESUME lands while paused), CONNECTION LOST freeze+card on transport failure or EV_BYE, 2 s "LEVEL N - NEW X" banner via EV_GENERATION_START, dead-man releases held actions after 1 s of input silence, REJECT UX was in since Phase 5. **Native↔web cross-play VERIFIED**: native host (Xvfb) + headless-Chromium web client through the real lobby with the clipboard bridged via xclip — connected, played, both transports healthy (scratchpad `pwtest/cross_test.js`).

## Milestone 2 — room codes, signaling, rejoin, TURN, delta snapshots

### M2 decisions (locked with Glenn 2026-07-04)

- **Signaling host**: Cloudflare Worker + Durable Objects (Glenn's account; free tier). Code lives in `signal/` (worker + wrangler config); Glenn deploys with `wrangler deploy`; local dev/e2e uses `wrangler dev` (miniflare — no account needed). Default endpoint baked into the build, overridable via `signal_url` in the preferences INI.
- **Native WS client**: libdatachannel's built-in WebSocket (`rtcCreateWebSocket`) — flip `NO_WEBSOCKET=OFF` in `build_netplay_deps.sh` and `xbox/CMakeLists.txt` (TLS via the existing MbedTLS). Web: browser `WebSocket` in the EM_JS glue.
- **Room codes**: 5 chars (Glenn, was 4), server-assigned, from a 29-char unambiguous alphabet (no 0/O/1/I/5/S, no F = fullscreen key), case-insensitive entry, ~2 h TTL.
- **Signal protocol**: JSON text frames — `{t:"create"}` → `{t:"room",code}`; `{t:"join",code}`; relay `{t:"offer"|"answer",sdp}`; `{t:"peer",ev}` notifications; `{t:"err",reason}`. SDP stays **non-trickle** (the 8 s gathering fallback bounds code latency); trickle is a later optimization now that a live channel exists.
- **Clipboard flow stays** as the fallback path when the signal server is unreachable.
- **Rejoin**: the room outlives the client — on client loss the host keeps playing and reopens the room slot; a rejoining client is just a fresh join (any snapshot bootstraps it).
- **TURN**: Worker endpoint mints short-lived Cloudflare Calls TURN credentials; both backends add them to the ICE config.

### M2 phase status

- [x] **M2-0 — spike**: `signal/` Worker (Durable Object room, WS relay) running under `wrangler dev`; native deps rebuilt with WebSocket ON; C-API WebSocket echo against the local worker; web-page WS echo. Housekeeping: retire `spike/netplay/` + `netplay-spike.yml` (superseded since Phase 2). **DONE** — relay verified twice over: node WS client (scratchpad `signal_test.js`: code mint, lowercase join, offer replay to late/re-joiners, answer relay, room-full, leave→slot-reopen, no-such-room) and libdatachannel `rtcCreateWebSocket` C spike (room→join→offer relay). `wrangler dev --local` works in the dev container (miniflare; `NO_PROXY=127.0.0.1` for clients). `build_netplay_deps.sh` now builds with `NO_WEBSOCKET=OFF`; game rebuilds and NET SELFTEST still PASS against the WS-enabled lib. (Web-page WS echo deferred to M2-1's web backend — browser WebSocket is not a risk item.)
- [x] **M2-1 — NetSignal seam**: `net_signal.h/.cpp` interface + native (rtcWebSocket) and web (EM_JS WebSocket) backends; JSON codec; self-test hook. **DONE** — `NetSignal` (connect_host/connect_join/send_offer/send_answer/poll(Event)/close; events Room/Joined/Offer/Answer/PeerJoin/PeerLeave/Error/Closed), hand-rolled JSON escape/field/parse (protocol is ASCII; \uXXXX handled), `net_signal_url()` (NEWTONIA_SIGNAL_URL env override, baked default placeholder until M2-3), room-code alphabet helper for the entry UI. `NEWTONIA_SIGNAL_SELFTEST=1` hook in glut.cpp + xbox_main.cpp. **SIGNAL SELFTEST PASS** against wrangler dev through the full public API (room mint → join → offer/answer with CRLF payloads round-tripped through the JSON codec). Web backend emcc-syntax-checked; empty-TU check on non-net builds green.
- [x] **M2-2 — lobby rooms**: HOST shows "ROOM CODE ABCD"; JOIN gets a 4-char entry screen; automatic offer/answer through the room; e2e vs local relay (Xvfb native, Playwright web). **DONE** — new lobby screens RoomHost (big code + WAITING FOR PLAYER 2), CodeEntry (`Y K - -` slots; V/C/W/S are valid code letters so entry mode bypasses the shortcut keys), RoomJoining; `pump_signal()` pushes the offer when gathering yields it and reacts to relay events; NO candidate stripping in the room flow (both ends live — full offer, ICE starts both sides at once; the strip stays in the clipboard path). Manual clipboard flow kept as automatic fallback (`fall_back_to_manual`) on signal-server timeout (12 s), refusal, or socket close; join errors (no-such-room/full/expired) bounce back to CodeEntry with a message. Web builds resolve the signal URL via `Module.NWTN_SIGNAL_URL` or `?signal=` query (no env vars in browsers). **Verified**: native↔native Xvfb e2e (scratchpad `drive_room.sh` — reads the code from the host log, types it into the joiner; both in-game, joiner flying in the host's world) and web↔web Playwright (`pwtest/room_test.js` — two tabs, code typed through the real UI, joiner bootstrapped into the game). No clipboard involved in either.
- [x] **M2-3 — deploy**: Glenn runs `wrangler deploy`; bake the production URL; cross-machine + cross-play verification over the real internet. **Deployed** — `wss://newtonia-signal.gfmcc.workers.dev/ws` baked as `SIGNAL_URL_DEFAULT`. Deploy notes: Cloudflare's dashboard wouldn't enable the hello-world Deploy button without a workers.dev subdomain and wouldn't offer the claim UI (chicken-and-egg, error 10063 from wrangler); registered the subdomain directly via `PUT /accounts/{id}/workers/subdomain` with the Global API Key, then `npx wrangler deploy` sailed through. The dev container's egress proxy blocks TLS to workers.dev, so production verification runs on real machines: `NEWTONIA_SIGNAL_SELFTEST=1 ./newtonia` (defaults to the baked URL) → SIGNAL SELFTEST PASS expected, then a real room-code session between machines.
- [x] **M2-4 — rejoin**: host reopens the room on client loss; client rejoin flow; verified mid-game drop/reconnect. **Design (locked)**: the host GLGame adopts the lobby's NetSignal (room stays open all game); on client loss the host keeps simulating solo ("PLAYER 2 DISCONNECTED - ROOM ABCD OPEN" notice), parks the remote ship (kill_stop + frozen respawn timer so it can't bleed lives), builds a fresh NetTransport and pushes a new offer into the room; a rejoining client is a plain JOIN with the same code (full snapshot bootstrap — zero new client code), on whose arrival the host swaps in the new session, re-baselines the INPUT counters (net_have_input_=false, held suppression armed), unfreezes + respawns the remote ship (time_until_respawn=0), and play resumes with the joiner's score/lives/weapons intact. Worker needs no changes (slot reopen + offer replay shipped in M2-0). e2e: connect, SIGKILL the joiner mid-game, relaunch, rejoin same code, verify score/lives survive. **DONE & VERIFIED** (scratchpad drive_rejoin.sh): host prints "player 2 lost - room reopened" then "player 2 rejoined"; rejoined ship alive with all 4 lives (unpark respawns explicitly — the step-countdown respawn would charge a life). Gotcha: the rehost block must trigger only on a DEAD-or-absent session, not on net_connection_lost_ alone, or it destroys the fresh handshaking session created from the rejoin answer (first e2e failed exactly there). SIGKILL detection takes several seconds (no clean close — waits for ICE failure), so the host notices the loss late; fine.
- [x] **M2-5 — TURN (code side)**: credentials endpoint + ICE config plumbing in both backends. **Landed; awaiting Glenn's TURN key to activate.** **Design**: Cloudflare Calls TURN. Glenn creates a TURN key in the dashboard (Calls -> TURN), then `wrangler secret put TURN_KEY_ID` + `wrangler secret put TURN_API_TOKEN` and redeploys. The worker mints short-lived creds per connection (POST rtc.live.cloudflare.com/v1/turn/keys/{id}/credentials/generate, ttl ~2h) and sends them as flat `{t:"ice", urls, username, credential}` frames (one per server) BEFORE the room/joined frame; without the secrets it sends none and clients stay STUN-only (graceful). Client: NetSignal Event::Ice; NetTransport::set_ice_servers() (native: "turn:user:pass@host:port?transport=udp" strings in rtcConfiguration; web: RTCIceServer objects) — must be called before pc creation, so the HOST defers start_host() until the Room frame arrives (joiner already starts at the Offer, after Joined). Manual fallback keeps STUN-only.
- [x] **M2-6 — delta snapshots**: keyframe + per-object delta encoding, bandwidth counters, late-game measurements. **Design (locked)**: measure first (periodic host log of snapshot bytes; e2e skipped to late generations). Then: KEYFRAMES stay the exact current full snapshot (savegame body + extras — bootstrap/rejoin keep working unchanged) but drop to 1 Hz; between them, MSG_DELTA (reliable channel) at 10 Hz carries players + ship extras in full (small) plus per-asteroid DYNAMIC records only (net_id, pos, vel, rotation, health, transient state bytes) for asteroids the client already knows, full records only for ids never sent — the reliable ordered channel means the host KNOWS what the client has received, so no acks: host tracks the sent-id set, removals stay implicit (ids absent from the delta are killed, as today). Pickups/black holes/station ride in the delta wholesale (small). PROTO_VERSION 2→3. **MEASURED (telemetry now in net_host_send_snapshot, a line per 100 snapshots)**: gen 2 / 27 asteroids = 2.5 KB/snap = 10 KB/s; gen 13 / 164 = 14 KB = 114 KB/s; gen 24 / 327 = 28 KB = 274 KB/s sustained (~2.2 Mbps) — asteroids dominate at ~80 B each. **Refinement**: deltas carry an asteroid record ONLY when its velocity or transient state changed since last sent (bounce/hit/teleport/phase) or the id is new — straight-line drift is client-extrapolated (already stepped) and trued up by the 1 Hz keyframe. Projected steady state at gen 24: ~60 KB/s (keyframe 28 KB + near-empty deltas). **LANDED & MEASURED: gen 24 = 274 KB/s -> ~33 KB/s (8.3x); gen 2 = 10 -> 2.8 KB/s.** Deltas run 260-800 bytes carrying 0-16 dynamic asteroid records. Verified: bandwidth e2e through 24 generation changes, room e2e (joiner synced in the host world on deltas), rejoin e2e (forced keyframe on session swap). Bonus fix found by the run: fall_back_to_manual() called from inside pump_signal's poll loop deleted signal_ and the loop kept polling it (use-after-free crash whenever the relay socket closed pre-room, latent since M2-2) — the loop now returns immediately after any in-loop fallback. **Implementation spec**: PROTO_VERSION 2->3; new MSG_DELTA=7 (reliable, single message; if a built delta exceeds SNAPSHOT_CHUNK_BYTES the host sends a keyframe in that slot instead — escape hatch, no delta chunking). Every 10th snapshot slot (and the first, and after a rejoin via net_force_keyframe_) is a KEYFRAME = today's full chunked snapshot, at which the host RESETS its per-client asteroid baseline map net_known_ (net_id -> {pos, vel, health, state byte, sent time}) to the keyframe contents. MSG_DELTA body reuses the whole existing pipeline: (a) serialize_game() of a mini GameState with the asteroid vector EMPTY (players/pickups/black holes/station/mini all ride wholesale — small); client applies it via net_apply_state(s, apply_asteroids=false) (new default-true flag; empty list must NOT kill asteroids); (b) the ship half of NetExtras (net_apply_extras split into ship part + asteroid-id part so deltas reuse the ship part); (c) asteroid delta section: u16 n_new {net_id + full Save::Asteroid record — split children appear here}, u16 n_dyn {net_id, pos 2f, vel 2f, health u8, state u8 bits: phased/teleport_vulnerable/teleport_pending/quantum_observed}, u16 n_removed {net_id — client kills like the absent-id path today}. DIRTY RULE per known asteroid: |vel - vel_sent| > 0.01, health changed, state byte changed, or |pos - (pos_sent + vel_sent*dt)| > 50 (catches teleports); baseline updated whenever a record is sent. Client defensively ignores dynamic records for unknown ids (next keyframe reconciles). Client additionally applies black-hole gravity to non-invincible asteroids in tick_net_client (mirrors the host loop, ignore swallow return) so gen>=9 drift stays small between keyframes — otherwise gravity dirties every asteroid every delta. Save:: needs its per-struct player/asteroid writers exposed (they exist as internal helpers in savegame.cpp) for the delta player/new-asteroid records.

## Milestone 3 — mobile netplay (plan, locked with Glenn 2026-07-05)

Scope decisions (Glenn 2026-07-05): **Xbox netplay is DEFERRED to a future milestone** — store-path online multiplayer drags in Xbox Live cert requirements (multiplayer privileges, parental controls, anonymous-peer policy) that change the architecture, so it gets its own milestone when the time comes (dev-mode-only play is the likely first cut; the controller letter-picker for code entry lands there too). M3 is Android/iOS.

- [x] **M3-1 — mobile lifecycle & host room reclaim** (worker + game; desktop-e2e-able before any mobile build exists). Today the worker expires the room the moment the HOST's signal socket drops — a mobile host switching to a chat app to send the code kills the session, and there is no host-side rejoin at all (only the client has one). Plan: (a) worker: on host socket loss, keep the room alive for a grace period (~2 min) with the host slot reclaimable — a reconnecting host proves ownership via a token minted with the room frame (random uuid, kept in game memory); joiner slot logic unchanged; (b) game host: on signal loss or app resume, reconnect to the room with the token and re-offer (mirror of the existing client rejoin); (c) client: platform resume events (SDL_APP_WILLENTERFOREGROUND / focus_gained) trigger the existing rejoin automatically instead of showing CONNECTION LOST; (d) e2e under Xvfb: kill/restart the host's relay socket mid-game, verify the room survives and both reconnect paths work.
- [x] **M3-2 — touch lobby + code entry** (landed 2026-07-05; desktop lobby e2e green — keyboard flow untouched; on-device verification rides M3-3/M3-4 since no mobile build exists yet):
  - **(a) ONLINE on touch** — landed as a full-width band, then superseded the same day by the device-testing rework (session (t)/(y)): touch now shows the desktop-style attract screen ("tap to start") that dismisses into a vertical row menu — CONTINUE / NEW GAME / ONLINE, equal sizes, spread over a tall band (160..-300, high score moved down) — with `menu_row_at()` mirroring `draw_menu_rows()` so taps land exactly on the drawn rows.
  - **(c) lobby touch targets + soft keyboard** — as reworked through device testing (sessions (t)/(v)/(dd)): Choose = upper/lower half taps with HOST/JOIN spread apart, no "> " cursor on touch; CodeEntry re-summons the keyboard on tap, draws heading/code/hint in the TOP half (the keyboard covers the bottom), flashes "CODES NEVER USE 0 O 1 I 5 S OR F" on filtered letters; RoomHost spreads over the full height with the share band anchored to the drawn "TAP HERE TO SHARE IT" line; every lobby screen has a RETURN TO MENU bottom tap band; the joiner waiting screens add a CANCEL band → empty join screen; LobbyFailed = TAP TO TRY AGAIN → empty join screen. Keyboard input: on Android, KEYDOWN is the single source of typed characters (soft keyboards deliver via both commitText-synthesized key events AND real IME KeyEvents; TEXTINPUT is not forwarded, and `SDL_StopTextInput()` runs at startup because SDL starts with text input ACTIVE); iOS keeps the TEXTINPUT path with printable-KEYDOWN suppression. Code entry consumes on key-DOWN in `NetLobby::keyboard()`; key-ups are ignored on touch (the finger zones synthesize them — the stray-P bug).
  - **(d) share seam + clipboard hardening**: `net_share_text()`/`net_share_available()` seam (no-op default hoisted OUTSIDE the `__EMSCRIPTEN__` guard — web links against the no-ops and uses the clipboard wording; Android JNI ACTION_SEND chooser in android_main.cpp; iOS UIActivityViewController in **ios_share.mm** — a separate TU because UIKit's MacTypes.h defines a global `struct Point` that collides with the game's). RoomHost touch band + desktop keeps clipboard-only. Android 10 reads the clipboard only with window focus: the CodeEntry auto-join poll retries an empty clipboard for ~2 s before giving up; the auto-join also refuses the code THIS instance hosted (`s_last_hosted_code`) and is not re-armed by TRY AGAIN/CANCEL retries.
- [x] **M3-2b — trickle ICE** (LANDED 2026-07-05; **measured: offer leaves 0 s after the room code (was 8 s — the full gathering fallback), joiner in-game 4 s after selecting JOIN (was ~16+ s); 3x stable, rejoin e2e green with trickle live**. Race found & fixed in verification: candidates can be gathered BEFORE the local description is ready, and a candidate sent before its offer gets wiped with the relay's stale-cand buffer when the offer lands — the game now only streams candidates after its SDP has gone out. Worker relays/buffers `{t:"cand"}` (6 new protocol-test checks); native uses rtcSetLocalDescriptionCallback + rtcSetLocalCandidateCallback + rtcAddRemoteCandidate; web mirrors with onicecandidate/addIceCandidate (EM_JS half not compilable in the dev container — verified by pattern against the existing glue; web.yml on master is the compile gate). The manual clipboard flow and old-peer path stay non-trickle. **Worker redeploy required WITH this client update**: an old worker drops the cand frames, and a trickle offer carries no candidates — the room flow then fails at the 25 s timeout instead of degrading. The manual fallback still works either way.) Cuts the up-to-8-s candidate-gathering wait out of every connect AND every rejoin — it compounds with M3-1's auto-rejoin and matters most on mobile networks, where STUN/TURN round-trips are slowest. Plan: new relayed `{t:"cand", mid, cand}` frame in the worker (pure relay, both directions, like offer/answer; buffered like the offer for a joiner arriving mid-gather); both backends send the SDP immediately and stream candidates via the per-candidate callbacks (libdatachannel `rtcSetLocalCandidateCallback`, browser `onicecandidate`), feeding remote ones in as they arrive (`rtcAddRemoteCandidate` / `addIceCandidate`); the non-trickle blob stays as-is for the manual clipboard flow (no live channel there) and as the compatibility path against older peers — a peer that never sends cand frames just behaves like today. Handshake-only change; gameplay traffic untouched.
- [x] **M3-3 — Android netplay enablement** (landed 2026-07-05; CI is the compile gate). Root CMakeLists gains the FetchContent MbedTLS+libdatachannel block (xbox pattern, same pins; NEWTONIA_NET option defaults ON so the existing android.yml compiles netplay for all three ABIs with no workflow change), links LibDataChannelStatic, defines NEWTONIA_NET_RTC; AndroidManifest gains INTERNET + ACCESS_NETWORK_STATE. **Runtime verification: Glenn tests on a real device via the CI APK.**
- [x] **M3-4 — iOS netplay enablement (CI compile+link gate landed 2026-07-05; device wiring is a documented manual step)**. ios.yml builds static MbedTLS+libdatachannel for the simulator (same pins/user-config, escaped-quote UCFG) and compiles+links the game with NEWTONIA_NET_RTC — the workflow hand-builds its source list, NOT the xcodeproj, so this is the real compile gate. Local/device builds: `build_netplay_deps_ios.sh device|simulator` + the three Build Settings steps in ios/README.md (define, header path, link the .a set); without them the project builds netplay-off as before. Discovered en route: UIKit's MacTypes.h defines a global `struct Point` colliding with the game's — anything importing UIKit must not see game headers (ios_share.mm precedent). **iOS verification boundary (kept honest):** CI compiles+links and runs BOTH self-tests in a booted simulator — loopback (transport/DTLS/data channels inside the iOS runtime) and signal (wss TLS + a full room round-trip against the PRODUCTION relay). **Still unverified on iOS**: the touch lobby / room-code UI flow, soft-keyboard code entry (iOS keeps the TEXTINPUT input path — different from the Android KEYDOWN fix, untested on hardware), real cross-device gameplay, and the DEVICE build via the Xcode project (`build_netplay_deps_ios.sh` + the ios/README.md Build Settings wiring has never been exercised) — all Glenn's Xcode/device session.

- [x] **M3-5 — deploy workflows carry netplay** (landed 2026-07-06; see session log). Every release artifact now builds with the netplay backend: deploy-itch and deploy-android already had it (web compiles the RTCPeerConnection backend unconditionally; the Android AAB uses the root CMakeLists where `NEWTONIA_NET` defaults ON — the same build android.yml gates); deploy-ios gains the device-arch MbedTLS+libdatachannel static build wired through new empty-expanding pbxproj vars (`NEWTONIA_NET_DEFINE`/`NEWTONIA_NET_HEADER_PATH` — the net_*.cpp sources are now in the Xcode project too, they never were); deploy-steam builds netplay on all three platforms (Windows: MinGW static libdatachannel with msys2 OpenSSL + `RTC_STATIC`; macOS: `build_netplay_deps.sh --universal` with the fat dylib bundled+signed in Frameworks; Linux: libdatachannel built inside the steamrt sniper container, shipped next to the binary under its SONAME via the `$ORIGIN` rpath). Every native deploy job runs the headless `NEWTONIA_NET_SELFTEST` loopback as a gate, and the previously-netplay-less dev CI (windows.yml, linux.yml) now builds the same recipes + selftest on every push — the pre-deploy proving ground, since deploy workflows only fire on tags/dispatch. ios.yml additionally xcodebuild-compiles the real Xcode project with the netplay vars (deploy-ios parity gate). **Shipped and confirmed**: `netplay-v1.41.0` deployed all four channels green (Steam `netplay` branch, TestFlight 1.41.0, Play internal, itch `html5-netplay`), and Glenn confirmed the Steam netplay-branch build runs netplay on a real Mac.

**Room-code management plan (locked):**
- **Hosting**: the big-type code display + clipboard auto-copy already work on mobile (SDL clipboard = OS pasteboard). Add a **native share-sheet button** on the room screen — Android `Intent.ACTION_SEND`, iOS `UIActivityViewController`, small JNI/ObjC bridges in the existing shells (`android_main.cpp`, `ios/ios_main.mm`).
- **Joining, layer 1 — clipboard auto-join** (shipped in M2 polish): copy the code from the chat message, open the game, tap JOIN — zero typing. M3 hardening: iOS shows a paste banner (fine); Android 10+ only allows clipboard reads with window focus, so the read may need a short retry after resume instead of firing on the first frame.
- **Joining, layer 2 — soft-keyboard entry**: covered by M3-2.
- **Joining, layer 3 — deep links / universal join link** (SHIPPED, field-verified on iOS + Android + Steam): host shares `https://newtonia.metonymous.com/join?code=<CODE>` (`net_join_url`, sent by both the SHARE band and the clipboard auto-copy — the clipboard is the only share affordance on desktop/Steam). The static `/join` page (`web/site/join/`) routes by device: installed iOS/Android apps intercept it via Universal / App Links (`ios_universal_link.mm`; `AndroidManifest` `autoVerify` + JNI), Steam desktop gets a `steam://run/4536720//+connect` button (consumed by `steam_invites.cpp`), everyone else falls through to `/play/?code=` (`web_main.cpp` `web_accept_invite` ← `web/main.ts`). All funnel into `Invites::note_accepted → poll_accepted_invite → NetLobby(code)`. The `.well-known/{apple-app-site-association,assetlinks.json}` association files are live from master with the real Team ID + Android SHAs. Pasting the link into JOIN also works (`room_code_from_clip` extracts the code). See CLAUDE.md "Universal join link" for the full wiring. Remaining human config: none — iOS Associated Domains and the Steamworks promptless `+connect` Launch Option are both configured and field-tested. The `/join` page's *no-app* fallback: iOS (public App Store) shows the Smart App Banner + a "Get the app" button (App Store id 6760685759, no silent redirect); Android (internal-testing only for now) and everything else fall through to the browser game.
  - **Follow-up (task #145), gated on Android going public**: add a Play Store button to `/join` and implement **install→auto-join** for a fresh Android install. Android can do deferred deep linking *for free* via the Play Store **Install Referrer** API — append the code as `&referrer=code%3D<CODE>` to the Play URL, read it on first launch (`NewtoniaActivity`, Install Referrer library) and feed `Invites::note_accepted`. iOS deferred deep-link is deliberately NOT pursued (no free mechanism — only paid attribution SDKs like Branch/AppsFlyer; the browser "Play now" path is the iOS one-tap answer). Also fill `assetlinks.json` with the public Play App Signing SHA-256.

Explicitly NOT needing new design (checked 2026-07-05): NAT/cellular (TURN covers CGNAT), late-game bandwidth on mobile data (~33 KB/s after delta snapshots), touch input plumbing (the touch joystick's analog values already ride MSG_INPUT), and app-store version skew (the PROTO_VERSION mismatch REJECT is already clean).

## Future milestone — host process-death resume (direction + implementation plan)

M3-1's reclaim token lives only in the host's memory: backgrounding/wifi blips are covered, but if the OS KILLS the host app (mobile memory pressure, crash) the token — and the world state — die with it. Resuming the room without its world is useless, so this feature is really "resume the whole hosted session", and it stays a future milestone until playtesting shows process death actually bites. Plan when it does:

1. **Persist the session ticket**: on every online auto-save moment (level clear, pause, client join/leave), write `{room_code, token, timestamp, PROTO_VERSION}` to a `netplay_resume` blob in the SDL pref path — alongside, not inside, the solo save.
2. **Online world save slot**: lift the `net_mode_` save gate for a dedicated `online_savegame.dat` written at the same moments (the existing serialize path works verbatim — it already feeds snapshots). The solo `savegame.dat` stays hard-gated; the two never mix.
3. **Worker: longer reclaim window for this case only if needed** — grace is in-memory in the Room DO and free to extend, but 2 min likely suffices for an app relaunch; measure first. No protocol change: reclaim is the same `role=host&code&token` call.
4. **Resume UX**: at menu boot, if a `netplay_resume` ticket exists, is younger than the grace window, and versions match → show "RESUME HOSTING ROOM XXXXX"; picking it loads `online_savegame.dat`, reclaims the room, re-hosts a transport, and waits for the client's rejoin (their auto-rejoin retries make this meet in the middle). Decline or expiry deletes both files.
5. **Cleanup rules**: ticket + online save deleted on clean session end (game over, BYE, returning to menu) so stale resumes never haunt the menu.
6. **Verification**: extend the M3-1 e2e — SIGKILL the HOST mid-game, relaunch within grace, drive the resume row, assert the client's auto-rejoin reconnects and score/lives/generation survive via the online save.

## Protocol quick-ref

Header: `uint8 proto_ver(=1) | uint8 msg_type | uint8 player_id | uint8 reserved`, little-endian, explicit byte packing.
Types: HELLO(1) C→H rel; WELCOME(2)/REJECT(3) H→C rel; INPUT(4) C→H unrel (uint32 seq, uint16 held bitmask, uint8 wrap-counters: boost/next_weapon/next_secondary/teleport/respawn_tap, 3 floats analog); SNAPSHOT_CHUNK(5) H→C rel (uint32 snap_id, uint16 idx, uint16 count, bytes); EVENT(6) rel (PAUSE/RESUME/GENERATION_START+gen/GAME_OVER/BYE); PING(8)/PONG(9) both ways unrel (uint32 sender ticks, echoed verbatim — 1 Hz RTT probe); HIT(10) C→H rel (uint32 asteroid net_id + uint32 bullet net_id — PROTO 13/14 client kill claim, always honored, killing bullet consumed by id); SHOT(11) BOTH ways rel (uint32 shot id, 2f spawn pos, 2f exact velocity, u8 flags — C→H PROTO 14: the host spawns exact clones instead of simulating the client's gun; H→C PROTO 17: the host echoes its own player's shots and the client spawns exact clones instantly with the shot sound, RTT/2-leaded to line up with the next 10 Hz bullet rebuild — host bullets used to pop in up to a snapshot interval late, obvious when spectating at the host's muzzle. The EV_REMOTE_SHOT p1 sound event is superseded. PROTO 18: flags bit2 = piercing beam bolt — clones plough through kills on both sides, and the snapshot's per-bullet records carry a flags byte so piercing/trail survive the 10 Hz rebuild; PROTO 19: the snapshot flags byte gains bit3 = world_bullet so host-side ricochets off reflective/armoured surfaces recolour white on the client too); HIT_SHIP(12) C→H rel (u8 kind: 0 enemy/1 station/2 mini, uint32 bullet net_id, uint32 target net_ship_id (0 for station/mini) — PROTO 16, 2f impact pos — PROTO 15/16, damage applies IFF the host consumes the referenced clone: exactly-once per shot; enemy claims name the exact enemy by wire id, absent = already dead = no-op; PROTO 20: bullet_id 0 = lance sentinel, enemy claims only, honored with no clone consume — kills are idempotent by id so the host's own MSG_LANCE polyline resolution can't double-count them); LANCE(13) BOTH ways rel (u8 n_points 2..17 + n x 2f polyline — PROTO 18, lance flash + sound on the peer, and since the lance-vs-ships feature the HOST also resolves a client polyline's ship/station hits from it (self on reflection, partner under friendly fire, enemies, stations) — ship damage is host authority, no extra message; a client lance's kills ride MSG_HIT claims with bullet_id 0 = no clone consume, its ray-march predicts outcomes and the claim drain kills locally; a host lance's kills replicate as removal records); BOUNCE(14) H→C rel (uint32 bullet net_id, 2f pos, 2f vel, u8 net_flags — PROTO 19 authoritative ricochet: the sim's real reflective/armour bounce overrides the client copy's local radial approximation so both screens fly the same post-bounce trajectory; unknown ids ignored).

## Trust model & input safety

P2P, no authoritative server — the "server" is the host *peer*, itself an
untrusted local binary, so validation is one peer defending against the
other, not anti-cheat. Because play is **cooperative** and **friends-invite**
(room code / share link), there is deliberately **no authoritative
re-simulation**: pose/aim/shots are client-authoritative by design (PROTO
12/13/14) to kill rubberbanding, and `net_host_poll` adopts them with *sanity*
checks only (finiteness + plausible speed — see the "not anti-cheat (co-op)"
comment). Score/kills are still hard to inflate: they are *derived* host-side
from exactly-once claims resolved against objects the host actually has (a
claim for a nonexistent/dead object is a silent no-op), never asserted by the
client.

What both peers DO enforce is **memory/CPU safety against a malformed or
flooding peer**: every wire field is bounds/finiteness-checked before use
(polyline counts are `uint8` bounded *before* alloc, velocities
magnitude-capped, ids looked up not indexed, event codes switched with a
no-op default). Both `net_host_poll` and `net_client_poll` carry two symmetric
per-poll budgets: a total read-loop cap (`NET_MAX_MSGS_PER_POLL` = 512) so a
message storm can't spin the drain, and a shared action budget
(`NET_MAX_ACTIONS_PER_POLL` = 64) across every droppable message. NEVER
drop-gated: the essential high-frequency stream (INPUT on the host;
DELTA/SNAPSHOT_CHUNK on the client) and **EVENT** — EVENT is reliable+ordered
and stateful (`EV_PAUSE`/`EV_BYE`/`EV_PICKUP` have no snapshot reconcile, and
a dropped one is consumed from SCTP forever; a legitimate post-stall backlog
can exceed the budget in one drain). EVENT's two work-doing codes bound
themselves instead: `EV_RAM_BLAST` (spawns bullets) and `EV_ACHIEVEMENT`
(pokes the platform SDK) share `NET_EVENT_EFFECTS_PER_POLL` (8), so an EVENT
flood degrades to sound cues + flag sets, all O(1). PING stays counted (bounds
the pong reflection; worst case the RTT lead stales until the flood ends).
Over budget the effect is dropped and draining continues; a legitimate frame
uses a handful of actions (a few shots + one INPUT). Below our read layer, the WebRTC
SCTP transport applies flow control / drops on the unreliable channels, so a
peer can't make us buffer unbounded data either. If leaderboard/achievement
integrity ever matters, validate at *submission* (and reuse the game-scoped
`cheated` flag), not per-packet in-game.

The two budget-exempt streams are safe to exempt because their per-message
work is bounded regardless of flood rate:
- **INPUT (host)** is O(1) — a seq check and a pose adopt (finiteness +
  speed-guarded, no alloc). Its wrap-counters (boost/next_weapon/
  next_secondary/teleport) are **clamped to 4** before the catch-up loop, so a
  fabricated counter jump can't loop-amplify into hundreds of actions; the
  only allocation is `new Teleport` (≤4/INPUT), a transient behaviour drained
  the same frame. Bounded by the read-loop cap.
- **DELTA/SNAPSHOT (client)** each do a full deserialize+rebuild, but the
  reassembler rejects any chunk whose `count` would exceed `SNAPSHOT_MAX_BYTES`
  (4 MB) and drops mismatched/never-completing partials, and `net_state_sane`
  bounds every deserialized count (asteroids ≤5000, pickups ≤500, …), so
  per-message allocation is capped. A flood is a *bounded CPU* cost (repeated
  rebuilds, ≤ the read-loop cap, and far less in practice — the reliable SCTP
  channel + chunking + bandwidth bound delivery well below it), never a memory
  or crash vector. Applying only the newest is deliberately NOT done: a
  superseded delta still carries monotonic NEW/REMOVED membership that must
  apply or kills are missed — capping rebuilds would risk that correctness for
  a griefing-only vector the victim can just leave.

The signaling Worker is a separate layer: it sits behind Cloudflare's edge
DDoS protection, holds only ephemeral per-room Durable Object state, and never
touches gameplay packets — a volumetric attack there is Cloudflare's problem,
not the game binary's.

## Verification checklist (M1 done =)

Two newtonia.exe on one machine: paste-connect, both ships controllable, remote one-shots work, host kills explode on client, pickups reflect, pause syncs, generation rollover on both, kill-process → CONNECTION LOST → Menu, solo save intact afterward. Then native↔web (Chrome+Firefox clipboard, chunking). CI: all three workflows green each phase.

## Release checklist (netplay → master → public, drafted 2026-07-17)

The feature set is complete and the deploy pipeline has shipped four
channels green from 1.41.0 through 1.44.5. What remains is verification
and configuration, not engineering. Order matters only where noted.

### Gate 1 — TURN decision (#110): CLOSED — staying on Cloudflare
- [x] **Decision (Glenn 2026-07-17): ship on Cloudflare TURN, stall bug
      and all.** Rationale: the cost-to-value ratio is huge (free at our
      scale vs a paid provider or a VPS to babysit), the client-side
      mitigations built during the relay work (SCTP RTO tuning, pose
      blending, gap drain, min-RTT lead) degrade the stalls to cosmetic
      glide on the observer's screen only, relay is already the rare
      CGNAT-fallback path, and Cloudflare has confirmed the bug and is
      fixing it upstream — so the worst case improves on its own.
      Revisit only if post-launch telemetry shows real pairs suffering
      (the gap forensics under NEWTONIA_NET_DEBUG stay in place).
- [ ] Sanity-check public-scale budgets: Workers free-tier request/DO
      limits and TURN minutes at expected launch traffic.

### Gate 2 — field pass on everything since v1.44.5 (none of it verified on-device yet)
- [ ] Tag `netplay-v1.44.6`; all four test channels green.
- [ ] Controller session (the nav/bindings refactor is the only change
      touching real input paths). VERIFIED on a local branch build
      (Glenn 2026-07-17): single R2 confirm, dpad nav, stick nav, menu
      nav, Options, CodeEntry picker. Remaining: quit/new-game confirms,
      Intro dismiss + B-to-menu, X paste on CodeEntry, and the
      held-stick-across-JOIN check (hold the stick down through the JOIN
      confirm — the picker cursor must NOT step on its own when the code
      screen opens; shared-latch fix 4204bff).
- [ ] Arrow keys drive P1 in gameplay and every menu; preferences.ini
      round-trips bindings on this build AND still parses on the stable
      branch (downgrade check — Steam testers share the file).
- [ ] Pause auto-save: play without dying, pause, force-kill, relaunch →
      CONTINUE resumes from the pause.
- [ ] God-mode expiry hands the trigger back to the weapon held at
      pickup (not the default gun).
- [ ] Kill-storm sanity on-device: mine carpet at gen 9+ on Android
      (native wide lines + batched score labels — should hold ~60 fps).

### Gate 3 — human platform config (Glenn-only, ~10 min each)
- [x] Steamworks: promptless `+connect` Launch Option — DONE (Glenn
      2026-07-17: already configured and tested; matches the Steam leg
      of the universal-join-link field verification).
- [x] Apple Developer: Associated Domains on `cc.gfm.Newtonia` +
      reprovision — DONE (Glenn 2026-07-17: iOS invite links already
      field-tested; a working universal link implies the capability +
      profile are in place).
- [ ] Steamworks portal: confirm `steam/rich_presence.vdf` tokens are
      pasted (App Admin → Community → Rich Presence).

### Gate 4 — decisions, not blockers
- [ ] Mobile-web perf posture: profile a mine storm on phone web (perf
      overlay + `?NEWTONIA_FRAME_LOG=1`); ship with known numbers or a
      documented caveat — WebGL keeps the line emulation, so web is the
      slowest renderer by design.
- [ ] Windows Defender: confirm Steam-installed unsigned builds aren't
      flagged (FP report was submitted; revisit signing only if so).
- [ ] Map the Game Center `coop_clear` achievement — netplay now makes
      2P earnable on touch (it was deliberately left unmapped).

### Gate 5 — release mechanics
- [ ] Merge netplay branch → master. Side effects to expect: production
      website deploys (landing page + `/join` + `.well-known` already
      live from master; web auto-join via `?code=` goes live with the
      merged `web/main.ts`), and master CI runs every workflow.
- [ ] Update the deploy workflows for master + default channels
      (task #152): deploy-steam's manual-dispatch default off the
      `netplay` test branch, itch off `html5-netplay` onto the main
      channel, audit ios/android for netplay-branch references, decide
      whether the `netplay-v*` tag namespace stays for future test
      cycles, and confirm tag-netplay.yml + the dev workflows' branch
      filters behave once merged.
- [ ] Tag `v1.45.0` (or next) — the normal `v*.*.*` pipeline: Steam
      `beta` branch, TestFlight, Play internal, itch. Promote channels
      (Steam default branch, Play production, App Store review) per
      platform once smoke-tested.
- [ ] Post-release: watch `wrangler tail` for room/TURN errors the first
      days; keep the netplay-v* tag namespace for future test cycles.

## Where things stand (2026-07-08 handoff)

**Architecture (PROTO 16), one paragraph:** the host owns the world —
asteroids, physics, rules, fragments, drops, score, everyone's fate —
and streams it as 10 Hz deltas + 1 Hz keyframes (reliable channel; the
client's stale gate sheds post-stall backlogs but keeps the once-sent
membership records). The client owns its own ship's entire presentation
and intent: pose and aim (12: the host adopts them from INPUT, the pilot
is never corrected), bullets (locally simulated, never echo-wiped, local
mirror-bounce approximation), gun (14: every shot reported via MSG_SHOT,
the host spawns exact clones — no independent spread rolls), kills
(13: MSG_HIT claims are always honored, killing bullet consumed by id,
predicted-kill suppression stops keyframe resurrection), and ship-target
hits (15/16: MSG_HIT_SHIP names the exact bullet AND exact enemy by wire
id, exactly-once clone consume on the host, instant local enemy death
with its own suppression map). Everything else the client mirrors
deterministically (gravity, elastic bounces, quantum observation) with
reconcile-and-glide corrections split between sim pose and render
offset.

**Known-good:** mac↔mac direct and relay-forced, Steam Deck↔mac, web
build compiles in CI. e2e drivers in test/e2e cover room/rejoin/
impacts/hiccup/blackout/mismatch; TESTING.md is the inventory.

**Open items (task list #s):** #110 CLOSED 2026-07-17 — staying on
Cloudflare TURN despite the stall bug (edge stalls 0.4-1.7 s, proven by
A/B vs metered.ca): the cost-to-value ratio wins, the client-side
mitigations degrade stalls to cosmetic observer glide, relay is the
rare CGNAT fallback, and Cloudflare confirmed the bug and is fixing it
upstream. See the release checklist Gate 1. Windows builds
are unsigned (Defender FP submitted; icon.rc now carries VERSIONINFO +
manifest; revisit signing only if Steam-installed builds get flagged).
Deferred niceties: host-side RTT lead on reported shot spawns.

**Glenn's on-device checklist (next playtest, all log receipts under
NEWTONIA_NET_DEBUG=1):** (1) gen-20 enemies — instant pop on the client
when your bullet lands, paired `ship impact consume kind=0 bullet=N
target=M` / host `ship hit claim honored (enemy M)`, plus a one-shot
`enemy wire ids live (first=N)`; (2) ~~gen-10 mini-station~~ CONFIRMED by Glenn in the field
2026-07-08: a client-killed mini now explodes on the client
(`mini-station death burst`); (3) gen 21+
— the sustained 66 ms hitching should be gone (if anything remains, the
new `slow tick: poll/steps` + `slow draw` lines name the phase);
(4) offline (wifi off + `bridge1` virtual interface, see (xx)) — JOIN
fails fast to "COULD NOT REACH THE ROOM SERVER", ENTER returns to
HOST/JOIN, and a manual invite copied at ANY point while sitting on the
join screen auto-enters the manual flow.

**Debug kit:** NEWTONIA_NET_DEBUG=1 (role-prefixed, timestamped),
NEWTONIA_NET_LOG_FILE=path (Windows), NEWTONIA_NET_RTC_LOG=info|debug
(libdatachannel/juice internals), NEWTONIA_NET_TURN="urls|user|cred"
(provider A/B), NEWTONIA_NET_FORCE_RELAY=1 / "0"-prefixed join code
(relay forcing), NEWTONIA_NET_SCTP_HB_MS (heartbeat interval, default
1 h). Gap forensics, tx-buffered markers, shot-flow counters, claim
logs and the hitch-breakdown lines (slow tick / slow draw) all live
under the debug flag — the session log below explains how to read each
one.

## Session log

- **2026-07-08 (bbb)**: **Host stuck on "CLEARED" (frozen countdown "6") with an asteroid still alive — client kill-claim races the level-clear check (Glenn: gen 0, all rocks killed by the client ramming).** Ordering bug exposed by PROTO 13: a client kill-claim is applied in `net_host_poll` (`a->kill()` → `num_killable--`), which runs BEFORE the once-per-tick level-clear check at the top of `tick()`; but the killed rock's breakup (`add_children`, which re-adds the fragments to `num_killable`) runs in the step-loop reap AFTER it. So a claim that empties the field dips `num_killable` to 0 for exactly one tick with fragments incoming — the check latches `level_cleared` (timer=5000, the overlay's `(t/1000)+1` renders "6"), then the fragments spawn and the countdown freezes (its decrement lives only inside the `num_killable==0` branch). The host's OWN bullet kills never hit this: they happen inside the step loop, after the check, so by the next check `num_killable` already includes the fragments. With the claimer dead (game-over testing), the un-counted fragments never die → stuck forever. The "ramming" kills are the client ship's death-`detonate()` bullets, which generate ordinary `MSG_HIT` claims. Fix: `Asteroid::pending_fragments()` (killed, not-yet-split, half-radius ≥ minimum) and the latch now defers while any just-killed rock still owes fragments — logs `net: level-clear deferred ...` on that tick. Verified: build + selftest + room progression green; a gen-0 claim-only clear driver (client rams/fires, host idle) advanced gen 0→1 on 21 claim kills with no stuck CLEARED — the old code would have latched and frozen.
- **2026-07-08 (aaa)**: **Client now has a GAME OVER state instead of silently disconnecting (Glenn: "no gameover state for the client, it just disconnects").** Two gaps: (1) the online game is always 2 players, so the offline game-over hints — which live in the single-player branch of `Overlay::title_text` — never drew on either screen; (2) the client's `tick_net_client` never ran the host's game-over bookkeeping, so `game_over_time`/`score_saved` stayed unset. Fix mirrors the authoritative host path over replicated fields (alive + lives both replicate, so both sides reach game-over on the same snapshot): `tick_net_client` now observes all-players-out (save high score, arm `game_over_time`, log `net: game over ...`), `Overlay::net_overlays` draws a shared "GAME OVER" + "PRESS/TAP FIRE FOR MENU" card that OUTRANKS the connection-lost card (the host leaving right after game over used to greet the client with "THE HOST LEFT THE GAME", so it never looked finished), and auto-rejoin is suppressed once `score_saved` (a post-game host departure is the expected exit, not a loss to recover from — stay on the card). The existing all-game-over→menu input handlers (keyboard any-key, controller A/B/X/Y, right trigger; all with the 3 s accidental-skip guard) already fire on the client since it plays with player-1 keys — they just needed `game_over_time` armed, which this does. Verified: build + selftest + room e2e green (no regression); live game-over needs Glenn's playtest (no headless way to zero both players' lives) — receipt is the `net: game over` client log line.
- **2026-07-08 (zz)**: **Client now shows the mini-station's death explosion (task #114, Glenn: "the small station explosion doesn't display on the client").** The death VISUAL is `GLMiniStation::destroy()`'s 120-particle radial debris burst — host-only until now, because the client's apply deleted the replica the moment the record said dead (`present && alive` gate) and `EV_STATION_BOOM` only carries the sound. Three-part fix in the client path: (1) the replica now lives for the record's whole `present` window (the host keeps its dead mini while the debris fades, so `present` stays true exactly that long); (2) the alive→dead record transition runs `destroy()` locally — BEFORE `restore_state` overwrites `alive` (destroy no-ops on a dead object), at the record's authoritative position, and only on a replica we watched die (a bootstrap mid-fade starts dead, no burst at a stale pose); (3) `net_client_step` upgraded from `Object::step` to `CompositeObject::step` — identical plus the debris animate/prune loop, without which the burst would freeze. Field-verifiable via the new `net: mini-station death burst (replica destroyed locally)` client log line, paired with the host's kill (its own sim or a `ship hit claim honored (mini-station)`).
- **2026-07-08 (yy)**: **Gen-21+ client frame hitches root-caused and fixed (task #115, Glenn's log: sustained `frame hitch 66 ms` 10/s) — plus a latent PROTO 16 bug the investigation exposed.** New permanent debug kit: `net: slow tick: poll N ms, K steps M ms` (tick_net_client phase split) + `net: slow draw N ms`, which localized the 66 ms instantly: ~48 ms in POLL on every 10 Hz delta, steps and draw innocent. Two stacked causes, both in the apply path: (1) `net_apply_state` runs on every delta and wholesale delete+recreated ALL GLEnemy replicas from the station record; (2) far worse, EVERY `Ship` construction loaded and decoded 13 WAVs from disk (`Mix_LoadWAV` into per-instance chunk members), plus 1–2 more per Weapon ctor — ~50 ms per ship, so one enemy deploy hitched a frame and the 10 Hz rebuild hitched EVERY frame; the per-apply player weapon rebuild (#71's churn) paid it continuously too. Fixes: `GLStation::restore_state` now reconciles enemies IN PLACE in list order (create only on growth, delete only the shrunk tail; a save-load lands with an empty list where reconcile degenerates to the old appends — GLGame's wholesale clear removed); new `sound_cache.h` `load_wav_cached()` decodes each WAV once and hands out `Mix_QuickLoad_RAW` wrappers (per-instance volume semantics preserved — chunk volume is live playback state, the wrapper keeps each ship's knob its own; `Mix_FreeChunk` in the existing dtors stays safe, allocated=0), converted Ship's 13 loads + all weapon ctor loads. Gen-22 driver: 26 hitches → 1–2, slow ticks zero. **The exposed bug:** PROTO 16's id mint sat in the `Enemy` constructor — which nothing instantiates (`GLEnemy` wraps a plain `Ship`; grep `new Enemy(` = zero hits), so every enemy rode the wire with id 0 and "exact" claims would have collapsed onto the first alive enemy. Mint moved to `GLEnemy`'s ctor; one-shot `net: enemy wire ids live (first=N)` on the client proves nonzero ids in the field (driver shows first=1). room/impacts/selftest green.
- **2026-07-08 (xx)**: **Lobby: unreachable room server no longer strands the joiner (task #105, Glenn's wifi-off report: "the host reverts to the old token copy paste but the client still sits on a normal join screen").** The joiner's signal-death paths (socket closed / 12 s `SIGNAL_TIMEOUT_MS`, non-rejoin) used to reuse the HOST's escape — `fall_back_to_manual` — which silently discarded the typed code and landed on `JoinWaitOffer`, a screen that reads as a normal join step and never finishes. New `join_unreachable()`: fail to `LobbyFailed` with "COULD NOT REACH THE ROOM SERVER" + a CHECK YOUR INTERNET CONNECTION advisory; ENTER on ANY LobbyFailed now returns to the HOST/JOIN chooser (Glenn — a failed JOIN used to rebuild an empty join screen via `retry_join()`, whose deliberate auto-read suppression made it "the OLD join screen"; re-entering JOIN from the chooser is one press and arms everything fresh, so `retry_join` is gone), rejoin mode keeps its budgeted retry loop while reconnecting, and the host's manual fallback is untouched. The manual pairing stays alive through a better door than the old accidental one: the CodeEntry clipboard read (auto on entry AND controller-X paste) now recognizes a manual INVITE blob — `decode_signal`=='O', matches nothing but our own format — and enters the manual flow directly (signal dropped, trickle off, `start_join` on the stripped offer). That auto-read path matters because desktop keyboards have NO explicit paste on CodeEntry (V is a code letter; only controller X pastes) — gating on explicit would have deleted the keyboard user's manual flow entirely. A joiner who copies the host's blob AFTER failing presses ENTER (chooser) → JOIN and the auto-read picks it up. **Follow-up (Glenn's offline post-game re-pair):** the join screen's clipboard read was one-shot (on entry), so an invite copied AFTER the joiner was already sitting there — the natural ordering when a manual game ends and the host re-hosts — was never seen. The join screen now RE-POLLS the clipboard while idle (800 ms cadence; desktop native only — web reads need a user gesture, Android toasts on every read; disabled once typing starts), so a late room code or manual invite is picked up within a second, no input needed. Verified by reordering the lobbyfail.sh driver: joiner parks on JOIN with an empty clipboard first, host's invite lands later, joiner enters the manual flow untouched. Verified with a 4-part driver (scratchpad lobbyfail.sh, dead-port signal URL): joiner fail-fast (log `join failed:` + screenshot of the failed screen, and NOT `manual fallback`), host manual fallback + invite copy intact, blob auto-pickup (`manual invite found at code entry`), and the full manual pairing to `bootstrap adopted` — plus room.sh and rejoin.sh green against the live relay.
- **2026-07-08 (ww)**: **PROTO 16 — per-enemy wire ids: exact enemy claims + instant local enemy kills (Glenn: "implement per-enemy ids", closing the two compromises (vv) documented).** Enemies were the last actors without wire identity — `GLStation::restore_state` RECREATES every `GLEnemy` replica from the station record on each apply, so identity was list order only; that forced (vv)'s nearest-alive-within-250 host lookup (wrong enemy in a furball) and ruled out client-side death prediction (resurrection flicker). Now: `Ship::net_ship_id` minted in the `Enemy` ctor (`++net_next_ship_id`; the host's mint is authoritative), ridden in the extras enemy section (u32 before each enemy's bullets) and RE-STAMPED onto the replicas on every apply — keyframe order is safe because the extras walk runs after the station restore rebuilds the list, and both sides serialize the list in the same order. `MSG_HIT_SHIP` gains `target_id`; the host's kind==0 handler drops the 250-unit proximity search for an exact id lookup (absent/dead = another resolution beat the claim = silent no-op — the kill still counted exactly once), and after honoring a claim erases the ship from `Ship::net_booms` so `EV_WORLD_BOOM` doesn't double the claimant's locally-played cue. Client side, enemy contact is now an INSTANT local kill (`kill_stop`+`detonate`+explode sound in `net_cosmetic_ship_impacts`) with a second suppression map (`net_predicted_ship_kills_`, 3 s expiry) holding `alive=false` through the re-stamp until the authoritative death replicates — the exact mechanism asteroid kills got in PROTO 13. Station/mini keep the rate-limited thud (they don't die per-hit). Verified: build + selftest + room + impacts green, plus a new gen-22 driver (scratchpad enemyids.sh, from the (47)-era station driver) proving the widened extras format round-trips with live enemy waves on the wire — 23 warp-snapped applies, zero parse errors. Positive-contact check for Glenn unchanged from (vv): shoot gen-20 enemies, expect client `ship impact consume kind=0 bullet=N target=M` + host `ship hit claim honored (enemy M)` pairs — and the enemy should now pop on YOUR screen the instant the bullet lands.
- **2026-07-08 (vv)**: **PROTO 15 — client bullets vs enemy ships / stations (MSG_HIT_SHIP; usage extended, Glenn's back; also: Cloudflare CONFIRMED the TURN bug and is fixing it with high priority — re-test relays when they announce, #110's economics may flip back).** Closes the (tt) known divergence Glenn queued up (his report was based on my handoff note, not a field sighting — the pass-through is certain by construction though: enemies/stations aren't in the asteroid grid, so client-owned bullets tested against nothing ship-shaped; the "host clones mostly miss moving enemies at stale replicated poses" half is geometry-inference, unverified). `Ship::net_cosmetic_ship_impacts(targets)` — the ship-shaped twin of the asteroid consume: the local ship's bullets check ALIVE enemies/station/mini-station replicas each step, consume at contact (spark + rate-limited thud) and claim via `MSG_HIT_SHIP(12)` {kind, bullet net_id, impact pos}. **Exactly-once rule, the design's core:** the host applies damage IFF it finds and consumes the referenced clone — a clone its own sim already resolved (hit something, TTL'd) makes the claim a silent no-op, so no shot double-damages the station and no ghost enemy dies; conversely the sim path can still resolve a shot the client never saw connect. Host application: enemy → nearest alive within 250 of the claimed impact, kill_stop + detonate + kill/score credit; station → hit() + host-local thud (NO EV_ROID_THUD relay — the claimant already cued, review caught the double-thud); mini-station → destroy + REWARD + boom sound/EV_STATION_BOOM relayed from the handler (the tick's boom block only fires for minis alive at its start). No client-side death prediction for ships (enemies restore from every delta — prediction would resurrection-flicker without per-enemy ids; deaths land ~RTT/2 via the normal replication). Verified: room/impacts/selftest green, asteroid claims unaffected, gen-10 blind spray produced zero false ship-impacts; positive contact needs aimed fire — **on-device check: shoot the gen-10 mini and gen-20 station/enemies, expect client `ship impact consume kind=N` + host `ship hit claim honored (...)` pairs.**
- **2026-07-08 (uu)**: **PROTO 14 — client-authoritative shot spawning (Glenn: "build PROTO 14", after spotting that "the bullets vector deviates randomly": weapon/default.cpp rolls rand() spread independently per side, so every shot's two copies flew on different headings).** The host no longer simulates the client's gun into bullets: each shot the local client fires is reported (`MSG_SHOT(11)`: id, spawn pos, exact spread-applied velocity, kills_invincible/trail flags; reliable+ordered so a HIT can never precede its bullet) and the host spawns EXACT clones via `Ship::net_spawn_reported_bullet`. The remote ship's weapon sim keeps running for ammo/cooldown/trigger bookkeeping but mints no bullets and plays no sim sounds (`net_remote_gun` gates in `Weapon::Default::fire/fire_shot` and `Ship::fire_bullet_from_gun` — the shot sound now plays when the real clone spawns). `MSG_HIT` gains the killing bullet's id, so the claim-consume is exact (nearest-at-impact stays as fallback for a clone the host sim already spent). Particle grows a `net_id`. Two hard-won findings from verification: (1) the default gun does NOT fire through `Ship::fire_bullet_from_gun` — `Weapon::Default::fire_shot` pushes bullets directly (and god-mode is the only fire_bullet_from_gun user), so the first cut hooked a path that never ran and the build silently behaved like PROTO 13 (caught because the new 1 Hz "reported shots/s spawned" counter stayed silent while claims still consumed sim-minted bullets); (2) the e2e drivers' `xdotool keydown space` produces ONE key press with no auto-repeat, so the semi-auto default gun fired once per 25 s spray — most historical "cosmetic impact" counts were the HOST ship's echoed bullets, not the joiner's. A tap-loop driver (60 presses) validated the pipeline at volume: 54 reports sent = 54 clones spawned (1:1), 4 kills claimed, 4 honored, all `(bullet consumed)` by id. Kept as production diagnostics: 1 Hz `shot reports/s sent` (client) / `reported shots/s spawned` (host) — a rate mismatch between them localizes a leak. The client now owns its ship's pose (12), aim (12), kills (13), bullets (tt) and gun (14); the host owns the world, the rules, and everyone else. room/rejoin/blackout/selftest green.
- **2026-07-07 (tt)**: **Vanishing bullets root-caused and fixed: the client's own bullets are now client-owned (no more wholesale echo rebuild).** Glenn reproduced on the Windows relay test (no log — Windows GUI/Steam launches lose stdout, hence the new `NEWTONIA_NET_LOG_FILE=path` redirect, engaged on the first NET_LOG). Code reading found the mechanism without the log: `nx_read_projectiles` rebuilt the LOCAL ship's bullet list from the host's echo on every apply (~100 ms) — any shot the host's simulated copy of the gun didn't fire (input still in flight, cooldown phase off by a step) got wiped mid-flight, exactly "bullets just disappear soon after they leave the ship". Worse, a wiped bullet can never reach its target, so the echo could still eat PROTO 13 hit claims. Fix: `own_bullets` — the local ship's plain bullets are never replaced by the echo; they're fully client-simulated (fired, stepped, TTL-expired, consumed at impacts, claiming kills). Consequences handled: reflective/armoured deflections are now approximated locally (velocity reflected about the contact normal — the echo used to paint the host's real bounce; the host's copy still does the gameplay-relevant one), and generation rollover clears local bullets explicitly (the quiet wholesale wipe used to). Known cosmetic divergences accepted: a locally-bounced bullet is claim-inert (net_sparked), and local bullets pass through enemy ships/stations visually (host sim still applies their damage). Mines/gigas/missiles/shockwaves stay host-echoed — deployed objects with host lifecycles, not aim-critical. The client now owns its ship's entire presentation: pose (12), aim (12), kills (13), bullets (tt). Verified: impacts e2e with claims flowing (1 kill → 1 claim → 1 honored), room, blackout, selftest, log-file redirect (host session logged only to the file).
- **2026-07-07 (ss)**: **PROTO 13 — client hit-authority: "make client kills always count, no more shots that don't count" (Glenn's call; the TURN swap parked as task #110).** The local ship's would-kill consume now does three things at once: kills the asteroid on the client THAT FRAME (instant, even mid-stall), sends a reliable `MSG_HIT(net_id)` claim, and records a 3 s suppression entry so a keyframe cut before the host processes the claim can't resurrect the rock (the keyframe create-branch skips suppressed ids; dyn/removal records for an absent id were already harmless no-ops). The host honors every claim: divergent host-side state is forced through rather than re-litigated (tough → health 1, teleporting → vulnerable, phased → solid; invincible is the one hard no — the client never claims those), then `kill()` + credit to the remote ship (score/kills/nova tally, the same block the host's own bullet path uses) and the normal reap spawns fragments/drops and emits the removal record — a no-op at the claimant, authoritative for everyone else. Host-simulated client bullets deliberately KEEP damaging: same-rock double resolution is a no-op, and the rare different-rock case (client bullet visually hit X while the host's copy hit Y) kills both — one generous shot in co-op beats collide-path surgery on the host. Duplicate claims within a tick (several bullets/steps consuming against the same rock before the drain kills it) dedupe via the suppression map. The old kill-confirmation telemetry is retired — its question ("does the kill land?") is now answered by construction; `hit claim sent id=N (killed locally)` / `hit claim honored id=N` are the new markers. Verified: impacts e2e — 3 blind-fire kills on the joiner, 3 unique claims, 3 honored (100%); room/blackout/selftest green. Score attribution note: the claimant sees its score bump ~RTT/2 later via the delta HUD scalars, same as always.
- **2026-07-07 (rr)**: **VERDICT: Cloudflare TURN convicted by provider A/B; the heartbeat conviction (qq) RETRACTED.** Glenn ran the decisive pair of sessions: with the heartbeat pushed to 1 h the Cloudflare gaps continued (new spacing fingerprint fits juice's 4-6 s jittered consent-check timer — observed diffs 4.21/4.59/5.01 and sums 9.9-10.2/14.6-15.8/19.9-20.2/25.2 are exactly 1-5 jittered periods, so (qq)'s "exact 10.000 s grid" was two HB periods of the same underlying interaction, not the HB itself)... and then **`NEWTONIA_NET_TURN` pointed at metered.ca: ZERO rx gaps in a full session.** Same game, same libdatachannel/juice, same consent checks, same wifi — only the relay differed. Whatever the mechanism (their edge slow-pathing periodic STUN? allocation-level buffering cycles?), the component is Cloudflare's TURN service; every app-side theory is moot and no further app-side mitigation is warranted. The 1 h heartbeat setting stays (harmless, marginally less traffic). Options forward: (1) switch the signal worker to mint metered.ca credentials (they expose an ephemeral-credentials REST API; free tier reportedly 20-50 GB/mo, paid $0.10-0.40/GB — Glenn's relay-forced testing burned ~320 MB in 3-4 h, and real players mostly connect direct), (2) self-host coturn on a small VPS (flat ~$5/mo, standard TURN REST HMAC credentials — the worker already does this dance for Cloudflare), (3) report to Cloudflare with the captured data (10.000 s HB-period grid, 4-6 s consent-period fingerprint, bidirectional zero-loss buffering, provider A/B) and keep an alternative meanwhile. Re-evaluations once a gap-free provider is in place: parked kill-authority #106 (stall-window shots were its motivation), vanishing bullets #108 (retest on metered).
- **2026-07-07 (qq)**: **THE STALL METRONOME NAMED: libdatachannel's SCTP HEARTBEAT default (10.000 s) — plus a UAF crash fix from Glenn's report.** The kit paid out in one session. Timeline analysis of his gap lines: gap STARTS sit on an exact 10.000-second wall grid (34.110 → 84.108 = 49.998 s across five periods; nine of thirteen gaps within ±0.35 s), with ICMP at 0.2 s intervals spotless through every gap. Eliminations: host process (slot telemetry drifts off-grid at ~10.35 s, no frame stalls), client process (no send stalls), radio/AWDL (clean ping), keyframes (1 Hz), juice timers (sources read: 15 s STUN keepalive, 4-6 s jittered consent, 540 s TURN refresh — nothing at 10 s), Cloudflare docs (no documented periodic behavior; UDP-only, so TURN-over-TCP is off the table for BOTH stacks). Then the match: libdatachannel's sctptransport.cpp enables SCTP heartbeats with `heartbeatInterval.value_or(10000ms)` — the exact metronome. Mechanism hypothesis: each HEARTBEAT on the relay path triggers an association-wide stall (lock convoy between usrsctp's timer thread and the juice agent send path, or a lost HB-ACK bumping path-error handling) — 0.4-1.7 s of bidirectional buffering, zero loss, exactly what every trace shows. Fix + conviction test in one: `heartbeatIntervalMs = 3600000` by default (data at 10-125 Hz both ways + RX watchdogs make HB redundant), `NEWTONIA_NET_SCTP_HB_MS` env override — setting 7000 must move the metronome to 7 s if the conviction is right; 0 restores the library's 10 s. **Crash fix (same session): use-after-free from Glenn's mac report** — a poll drain can delete asteroids (removal records, stale-membership kills) in one message and restore ships in a later one; restore_state runs respawn() → safe_position() → Grid::collide over the grid's now-dangling pointers (the crash stack verbatim). The delta-removal path now rebuilds the grid whenever it erased an asteroid (keyframe path already did) and net_apply_state rebuilds from the live list before any player restore, covering spark events and future consumers too.
- **2026-07-07 (pp)**: **Stall root-cause kit (Glenn: "I want to figure out this stall issue"; kill-authority question PARKED at his call — task #106 holds the design).** Field state: bidirectional 0.4-1.7 s dark spells on relay sessions, zero loss (everything queued+flushed), ICMP at 0.2 s intervals spotless straight through the gaps — flow-specific, and the gap durations (434/816/850/884/1739) sit suspiciously on the tuned SCTP RTO backoff ladder (min 200/max 1500), i.e. the stall may be usrsctp loss-recovery amplifying one or two lost-or-delayed packets at the relay into a full-association freeze. Also captured: the first `kill NOT confirmed` — a consume immediately before an 800 ms gap; the queued kill shot replayed on the host into a world 800 ms ahead, and the r=24 spd=0.122 rock had moved ~4 radii — stall-window shots structurally lose their kills under host authority (the parked question's motivation). New kit (2242e00): every NET_LOG line timestamped (seconds since start — periodicity fingerprints the layer: TURN permission refresh 240 s, channel rebind 600 s, ICE consent ~15 s); `NEWTONIA_NET_RTC_LOG=warn|info|debug|verbose` taps libdatachannel/libjuice's logger onto the same role+timestamped line stream (ICE/TURN/SCTP internals land next to the gap forensics); `NEWTONIA_NET_TURN="urls|user|cred"` replaces the worker-minted Cloudflare TURN for an A/B session against another provider (native only; libjuice speaks TURN-over-UDP only, so no turns:/tcp URLs). Mid-gap markers from (oo)'s follow-up commit e732fd5 print each side's tx-buffered depth the instant silence crosses 300 ms. Reading guide for the next session: (1) do gap timestamps recur on a period? (2) what do the rtc lines say in the 2 s before each gap? (3) mid-gap buffered on both sides = both senders blocked; (4) if stalls vanish on a non-Cloudflare TURN, the relay is indicted.
- **2026-07-07 (oo)**: **Gap attribution narrowing (Glenn's field data): radio exonerated, relay/SCTP under the lens.** His concurrent-ping test ("no pings over 60ms") during relay play with the gaps present rules out the mac's wifi radio and the AWDL theory — ICMP shared the radio and never stalled, so the stalls are specific to the game's flow (TURN path or the SCTP association riding it). The two field traces so far both show the same shape: bidirectional ~0.85-1.75 s dark, ZERO loss (0 seqs skipped; the full delta staircase arrives), everything buffered then flushed — which cannot distinguish local-vs-remote blocking because SCTP self-throttles when acks stop. New telemetry for the next round: `host:`/`client:` role prefix on every NET_LOG line (his ask; lib.sh room-code scrape switched to sed accordingly), and a 1 Hz `net: tx buffered N bytes` sample (NetTransport::buffered_amount — rtcGetBufferedAmount native, dc.bufferedAmount web) that only prints when nonzero: at these rates any nonzero means OUR sender is blocked because acks stopped. Kill-confirm telemetry from the same session: baseline hit→death 106-144 ms on relay, one outage-trapped kill at 906 ms, zero `NOT confirmed` — host/client hit agreement is solid; delivery timing was ever the only issue. Also found by Glenn while testing: wifi-off makes the HOST fall back to the manual token flow while the JOIN side sits on a dead code-entry screen (task filed).
- **2026-07-07 (nn)**: **The stale gate WEDGED SHUT — Glenn's "fundamentally out of sync, thinking all packets were stale" / "no packets are applying, i can't kill anything" (continuous `dropped stale delta (~200 ms behind)`, reconnect fixed it).** Root cause in (jj)'s estimate bookkeeping: `net_host_est_` was anchored to the accepted apply's host clock during poll AND `+= delta` in the same tick — any client-side hitch (mac beachball, window drag; his constant ~200 ms IS the hitch length) double-counted, leaving the estimate permanently ahead; with nothing ever accepted it never re-anchored, so every delta AND keyframe was dropped forever and only a reconnect (fresh GLGame) escaped. Fixed twice over: (1) the estimate is now DERIVED — anchor host-clock + local running time since the anchor arrived (pauses shift the anchor) — which cannot double-count; (2) a wedge hatch: 30 consecutive drops that are all <1000 ms behind (a real backlog drain rushes toward fresh; a wedge sits at constant mild staleness) force a re-anchor with a log line. Plus the stale-membership walk now logs loudly if its parse fails. Proven with a SIGSTOP-hitch driver (3× 450 ms joiner freezes): old logic wedges, new build takes 4 stale drops total and zero growth after; blackout.sh/room/selftest green (blackout's two "render jumps" this run were `tele`-flagged teleport relocations — deliberate snaps). **Also (nn): gap/kill forensics, no gameplay changes ("lets test it out, and not make presumptions")** — reading the new lines: host `input gap N ms ended (seq, X seqs skipped, Y stale rx during gap)` + `post-gap 1.5 s: A inputs accepted (~185 normal), B stale rx, max seq leap L` — seqs are minted every 8 ms client step, so skipped≈gap/8 = client kept sending into a dead path (lost); skipped≈0 + accept burst ≈ 2× normal = ordered backlog replayed (transit stall); skipped≈0 + normal-rate accepts + client-side `input send stall N ms` = the client stopped stepping (frame stall/app nap — the SIGSTOP driver produces exactly this signature). Client `rx gap N ms ended` says whether the outage was bidirectional. Kill claims: every local would-kill consume logs `cosmetic impact consume id=N` and starts a timer — `kill confirmed N ms after local hit` measures the real hit→death latency, `kill NOT confirmed 2500 ms after local hit (id/spd/r/hp)` is the "bullets hitting but not killing" smoking gun with the disputed asteroid's stats. A ≥300 ms gap implicates the reliable channel or the client itself, never pure unreliable loss (the 10 Hz reliable INPUT mirror caps a loss-only gap at ~200 ms).
- **2026-07-07 (mm)**: **Stale-gated deltas now keep their membership records (Glenn: "about a 1 second delay on ~1/20 asteroid hits", and his fix: "couldn't we just process the delta kill events when discarding them?").** The (jj) stale gate dropped whole deltas, but NEW/REMOVED asteroid records are sent exactly once (the host baseline forgets them on send) — a kill riding a gated delta waited for the 1 Hz keyframe backstop, hence the ~1 s late explosions on the ~5% of kills that coincide with a backlog. His approach beats the EV_ROID_KILLED event I was building: kills/breakups are monotonic facts (still true no matter how late the packet), so no PROTO bump, no new message — deltas are reliable, so the gate was the ONLY loss path. Implementation: on a stale delta the client now walks the stream instead of bailing — `net_apply_ship_extras(apply=false)` parses past the poisonous ship/projectile sections without touching anything (`nx_read_projectiles` takes `Ship*`, NULL = parse-only), then `net_apply_delta_asteroids(membership_only=true)` skips the stale dyn poses but applies n_new (breakup fragments spawn immediately, pose glide-corrected by later dyn records) and n_removed (kill + explosion, the existing removal path). Proven positively with a gen-10 driver (host sprays through 5×2 s total-UDP blackouts; black hole + mini-station as no-aim kill sources): 145 stale drops, `stale delta membership applied (+2 -1)` — a breakup's parent kill AND both fragments riding a 2.9 s-stale delta — plus three pure kills; the render jumps in that harsh run attribute to a teleport relocation (deliberate snap) and ship-warp drain-rate switches predating the first membership apply. blackout.sh (the committed 1.2 s regression profile), room, impacts e2e and the loopback selftest all green.
- **2026-07-07 (ll)**: **Deck round 2: ghost-world disconnect + double keyboard (Glenn's cross-device tag testing).** (1) "The mac player just travelled constantly on the client, no updates, eventually disconnected" = one-way path death (Deck wifi power-save the usual suspect); the retransmit-budget change (dd) had stretched transport-level detection to ~45 s of zombie world. Both roles now run a 10 s RX watchdog (host: INPUT silence, client: any-message silence; pauses re-baseline since paused peers legitimately go quiet) that declares the loss AND actively closes the transport — the close is what makes the surviving side fail fast and auto-rejoin; hiccup.sh caught the split-brain version that didn't close. (2) "Both keyboards are showing": the picker's hide-gate cleared `floating_kb_up_` on ANY controller event as dismissal proof, but Deck sticks stream drift/gyro AXIS noise straight through the floating keyboard — the flag died within ms of the summon. Proof is now a BUTTON press or an axis flick past STICK_ON. Also this hour: `net: relay-only ICE policy ACTIVE` log at pc creation (a mac .app launched by double-click never sees shell exports — that no-op was silent) and the (kk) remote-candidate filter it exposed. room+rejoin+hiccup e2e green, STEAM_BUILD stub gate passes.
- **2026-07-07 (kk)**: **One-sided force-relay was a NO-OP — Glenn caught it ("you wont like this": "relay-only ICE policy ACTIVE" followed by "ice path host/host").** libdatachannel's `iceTransportPolicy=relay` only filters which local candidates get SIGNALED; the ICE agent still fires connectivity checks from its own host sockets at every peer address it learns, so a one-sided forced session connected straight across the LAN. Consequences reassessed: every one-sided "relay" test was actually direct (including the Steam↔mac HOST/PUNCH session — my earlier "it's still relayed" explanation was wrong); BOTH-sides-forced tests (the TTL expiry runs, the 320 MB of TURN usage) were genuinely relayed, since neither side signaled direct candidates. Fix: relay-only now also DROPS non-relay REMOTE candidates (`add_remote_candidate`), so the only reachable peer address is the TURN endpoint — verified on loopback where no relay exists: the forced joiner logs the drops and correctly FAILS to connect rather than going direct. Plus `net: relay-only ICE policy ACTIVE (source)` at pc creation ends the silent-no-op failure mode (a mac .app launched via double-click/open does NOT inherit shell exports — launch from the terminal). Note for selftests: NEWTONIA_NET_SELFTEST + FORCE_RELAY fails BY DESIGN (loopback has no TURN). TESTING.md's one-phone claim ("connected at all = relayed") is now true for the right reason.
- **2026-07-07 (jj)**: **The forward-back-forward tour: stale post-stall backlogs now DROPPED (Glenn: "the asteroids visibly move forward, then back, then jump forward again. all of them at once").** The mechanism one level below everything else: during a ~1 s stall the client extrapolates forward correctly; at recovery SCTP trickles the queued backlog out over several frames as its congestion window reopens, and the early items describe the world of a second ago — applying them in order toured every asteroid backward then forward, all at once. In-order delivery of out-of-date states (his morning "out of order" hunch, vindicated). Fix: the client estimates the host game clock (`net_host_est_`, anchored by every accepted apply + advanced by local running time; pauses freeze both sides) and drops deltas/keyframes >120 ms behind it — dyn records are absolute and the 1 Hz keyframe re-syncs ids/removals, so a dropped item costs nothing; MSG_EVENT cues ride separate messages and are never dropped. Proven with `test/e2e/blackout.sh` (REAL 1.2 s total-UDP outages via iptables, ×3): the gate sheds the whole staircase (drops logged at 1426/1336/…/936 ms behind), ZERO asteroid render jumps, reconcile 1/s max 16, no snaps — the world sails through a blackout on extrapolation and picks up fresh. Also: a per-tick cap on the elastic separation push was tried and REVERTED (turned split-spawn pops, which explosion debris masks anyway, into sustained grinding on wedged pairs — worse). Answered: dropping stale items does not affect rebound/impact/pickup events (separate MSG_EVENT stream) or bullet-impact cues (client-side since PROTO 10).
- **2026-07-07 (ii)**: **Quantum mirror + fair-death drain + bullet lead + the last eye-proxy detectors (Glenn: "warp back and forth and end up in their expected location", "moving asteroids kill me when I didn't see the impact", jitter on the game-over screen).** (1) The back-and-forth signature = quantum observation flips: 4x/÷4 velocity changes computed host-only from player facings — the client extrapolated at the wrong speed each flip, oscillating rocks around their true paths (his offender logs were full of `quant` ids). `update_quantum_observation()` is now shared: host simulates, client mirrors per step — with gravity, elastic and quantum all mirrored, client asteroid paths are finally deterministic-given-records. (2) The rate-capped drain (hh) traded a lurch for lies: a big post-gap offset lingered ~1 s with the DISPLAYED rock up to 150 units from its COLLISION pose → unfair deaths. Drain is now proximity-scaled: full-speed within 700 units of the player (what can kill you is drawn truly), capped crawl beyond (no whole-field lurch). (3) Bullets (host/enemies/mini-station) rebuilt at RTT-stale poses every apply strobed against client stepping — led by RTT/2 at read; mines/gigas stay raw (disappearance matching). (4) New detectors complete the eye-proxy set: `net: SHIP render jump` (camera rides that pose — one hiccup moves the whole field in unison) and `net: frame hitch` (50-250 ms pacing stutters). Also this hour (earlier commits): asteroid render-jump detector, 50→16 dirty drift threshold + proportional-only separation push (the contact-pile sawtooth), rate-capped drain, RX-delay transport hook (NEWTONIA_NET_TEST_RX_DELAY_MS) and `test/e2e/fly.sh`. Verified: fly.sh under 2% real UDP loss + 30 ms RX delay, gen 8, 45 s of flying — ZERO asteroid render jumps, corrections max 60, 0 snaps; the only SHIP jumps are respawn warps. Triage protocol for any future report: jitter + asteroid/SHIP render-jump lines → paste them; jitter + silence → frame pacing (fps counter), not netcode.
- **2026-07-07 (hh)**: **The last two asteroid-jitter sources: unmirrored elastic bounces, and the 1 Hz rotation twitch (Glenn: "why are asteroids jittering at all? their path is deterministic" — it wasn't, client-side).** (1) Elastic asteroid-asteroid collisions (impulses at up to ~74:1 mass ratios + a positional separation push EVERY TICK while overlapping) ran host-only; every bounce was a surprise the records corrected 100 ms later. The pair pass moved into `elastic_asteroid_collisions(announce)` — host calls it for real (ting + EV_ROID_BOUNCE), the client mirrors it silently per step, like the gravity mirror. Gen-10 probe: 18-24/s → 1-18/s corrections. (2) The "constant jitter with QUIET position telemetry" was ROTATION: keyframes restored every rock's spin angle RTT/2 stale — a `rotation_speed × staleness` backwards twitch on every asteroid once per second that no position diagnostic measured. Matched asteroids now keep client-continuous `rotation`/`armour_angle` (rotation_speed still adopted, so no drift is possible). (3) Glenn asked for tests against the real relay — impossible here (no UDP egress), but the substitute is committed to TESTING.md: iptables 2% random UDP drop on loopback = real loss below SCTP; under it the gen-10 probe reads steady 4-9 corrections/s with glided loss bursts (max 172, 0 snaps) and zero input gaps at loopback RTT. Also this hour: asteroid glide budget 800 (post-stall bursts swoosh, real teleports still snap), SCTP maxRetransmitAttempts 30 (the RTO cap had cut association tolerance to ~12 s of loss — long outages now freeze-and-resume again instead of dropping to rejoin).
- **2026-07-07 (gg)**: **Gen-10 asteroid blowup near the black hole — the drain fed back through gravity; sim/render poses split (Glenn: jitter "resulting in player deaths").** His relay log read `reconcile 401 corrections/s (max 525, 16 snaps)` with dyn errs clustered at bh=37-310 — the drain kept the DRAWN pose offset from authority, but the drawn pose was also the SIMULATED pose, so the client's mirrored gravity was computed at the offset position; in the steep near-hole field the error re-amplified every step instead of draining, and the host killed players against rocks their screen showed hundreds of units away. `net_reconcile_pose` gains `sim_exact` (asteroids): sim pose = authority immediately (the gravity mirror tracks the host again), `Object::net_pose_err` becomes a RENDER-ONLY continuity offset added at the draw sites (AsteroidDrawer single+batch, invisible mask/star hiding/warp lens) and decayed by `net_decay_render_offset` without touching the sim. Remote ship keeps drain-into-position (no position-dependent forces client-side). Displayed rocks now converge to the host's collision poses, so dodges count. Gen-10 probe: 401/s max 525 (16 snaps) → 2-14/s max ≤92 (0 snaps); room+impacts e2e green. Also answered in-session: `wrangler tail`'s "Unknown Event - Ok" lines are tail's rendering of WebSocket/DO invocations (and "Alarm" = a DO alarm handler) — normal worker activity, not third-party use.
- **2026-07-07 (ff)**: **Feel-test verdicts (Glenn, mac pair): direct = "works perfectly"; relay = pilot smooth, observer glides.** Closes the (dd)/(ee) arc. With PROTO 12 + the host-side adopt glide (post-blackout catch-ups of 40-600 units bank into net_pose_err and drain ~150 ms on the HOST screen too — steady-state and teleport-scale reports still apply whole), a relay session under real loss shows: no pilot corrections at all, tiny drained world corrections (max ~27 units), input gaps down to ~250-670 ms (tuned SCTP RTO vs the stock ~1-1.7 s), and the only visible artifact is the lagging player's ship easing in on the OTHER screen. Direct connections show nothing at all. Implications for go-live: most real pairs connect direct or NAT-punched (relay is the CGNAT fallback), so the common case is the "perfect" one; the relay case degrades to cosmetic. Remaining diagnosis lever if relay loss ever needs chasing: Glenn's double-forced same-radio rig hairpins BOTH instances through the same TURN edge — a wired-mac relay run would split "wifi airtime" from "Cloudflare leg". Reminder that bit us: the TURN_TTL=90s test secret stayed deployed through several feel-test sessions (visible as `turn creds minted, ttl=90s` in wrangler tail — that log line exists for exactly this); delete + redeploy after expiry testing.
- **2026-07-07 (ee)**: **"Not playable" → two root-cause fixes: client-authoritative ship pose (PROTO 12) + SCTP RTO tuning.** Glenn's clean `ping` during the blackouts exonerated his wifi and convicted the TRANSPORT: all DataChannels — the "unreliable" INPUT channel included — share one SCTP association, and sparse game traffic rarely yields the 3 duplicate SACKs fast-retransmit needs, so one lost packet parks the whole association on the ~1 s stock retransmission timeout — exactly the measured 0.6–1.7 s gap durations. (1) `rtcSetSctpSettings` (native only; browsers expose no equivalent): min RTO 200 ms / initial 500 / max 1500, SACK delay 10 ms — native stalls drop ~5×. (2) The deeper fix, same design as the PROTO-4 client-authoritative aim: MSG_INPUT now carries the client ship's exact position/velocity + a `Ship::net_warp_count` echo; the host adopts the pose (finite/speed sanity only — co-op), collisions match what the player saw, and the client ignores the snapshot echo of its own pose outright — **the pilot is never corrected**, which also stops the camera slew that made asteroids look like they were rubberbanding (the camera is bolted to the ship). Warp gating: in-flight pre-warp INPUTs are ignored until the echo catches up, so respawns/teleports can't be dragged back; death stays host-authoritative via the alive transition; the client self-applies black-hole gravity (host's god/shield 0.25 scaling) since the host's pull is overwritten by every adopt. Blackouts now show only on the REMOTE view of the lagging player's ship, where they're cosmetic. room+rejoin+impacts e2e green, selftest PASS. Superseded/removed: the local-ship lead/dead-zone/blend/snap machinery ((aa)/(dd)) — reconcile+drain remains for world objects only.
- **2026-07-07 (dd)**: **Relay rubberbanding root-caused: ~1 s INPUT blackouts, confirmed by telemetry (Glenn: "a lot of jitter / rubberbanding", "i got synced ahead of my position").** His relay-forced mac pair logged local-ship corrections of 249-589 units at 16-27 ms RTT with NO warp-snap line — impossible from latency; the new host-side gap log then proved it: `input gap 1010/841/836 ms ended` pairing 1:1 with the big snaps. During such a blackout the unreliable channel dies (RTT spiking 16→207 ms says relay congestion, possibly macOS AWDL wifi scanning) while the host keeps acting on the last-heard HELD bits — release thrust as it starts and the host flies your ship onward until the 1 s dead-man: "synced ahead". Fixes: (1) input CHANGES (held bits / one-shot counters) now also ride the ordered reliable channel with a ~10 Hz refresh — a release can be late but never lost; same bytes+seq, the host's seq filter dedupes. (2) The local ship's 35% blend (visible multi-frame drag on a 249-unit gap) became the same bank-and-drain as world objects — velocity fully authoritative, pose glides ~150 ms. (3) `net_lead_ms()` now takes the MIN of the last 8 RTT samples — the smoothed average stayed poisoned ~10 s after one queueing spike, overshooting every extrapolation target. (4) Host logs `input gap N ms ended` (>300 ms) for correlation. room+rejoin+impacts e2e green. If gaps recur on calm networks, note the ~1 s periodicity pattern: macOS AWDL (AirDrop/AirPlay wifi scans) is the classic culprit — test wired or with AirDrop off before blaming the code.
- **2026-07-07 (cc)**: **Asteroid jitter ROOT-CAUSED and fixed with render-layer error smoothing (Glenn: "horrible jitter... snap out then back in, looks like out-of-order states").** New reconcile diagnostics (1 Hz summaries + per-record offender lines with hole-distance/speed/flags, all NET_LOG-gated) reproduced it headlessly at gen 10 with both ships parked: 40-80 corrections/s peaking at 260 units — NOT latency (loopback, 5 ms RTT), NOT the black hole (offenders 400-1800 units away), NOT out-of-order delivery (rel channel is ordered in both backends; a new snap-id guard logs+drops a stale delta if one ever appears — it never fired). The culprit: host-side elastic bounces — reflective/elastic asteroids swing speed 0.3↔0.7 between consecutive 100 ms records and plain rocks deflect off them, none of which the client can extrapolate; the (bb) positional blend fought that churn, extrapolating wrong then dragging back = the out-then-back-in look, and pre-(bb) overwrites were 10 Hz yanks. Fix: `net_reconcile_pose` adopts the authoritative VELOCITY exactly but keeps the render pose, banking the gap in `Object::net_pose_err`; `net_smooth_step` in the client tick drains it exponentially (~150 ms) so corrections glide monotonically; >250-unit jumps (teleports) still snap instantly. Applied to asteroids (keyframe + delta paths) and the remote ship; the local ship keeps its prediction blend. After: 3-21 corrections/s, max 11-65 units, visually smooth. room+rejoin+impacts e2e green, selftest PASS. The gen-10 probe driver technique (skip to gen 10, park both ships 20 s, read the correction summaries) is the template for verifying any future smoothing change.
- **2026-07-07 (bb)**: **"0"-prefix relay-only join + world-pose reconciliation + TTL mint log (Glenn's one-phone test session).** (1) Glenn asked how to relay-test on ONE Android phone: same-device peers always pick the direct ICE path, and `NEWTONIA_NET_FORCE_RELAY` is env-only (unreachable on Android/mobile web). Codes never contain `0`, so typing `0` before the code on the join screen toggles a relay-only join (`NetTransport::set_force_relay` — native ORs with the env var, web passes `iceTransportPolicy:'relay'`; status "RELAY-ONLY JOIN ARMED (TEST)"). Process-wide (like `s_last_hosted_code`) so the mid-game auto-rejoin after a TURN kick re-relays instead of silently going direct. One armed side suffices: it gathers no direct candidates at all. Recipe in TESTING.md ("One-phone relay test"): app hosts + browser joins in split-screen; connected-at-all = relayed. (2) Asteroids jittered ~1 Hz on the client even standing still: keyframes/delta records hard-overwrote poses RTT/2 stale over a delivery-jittery reliable channel — `net_reconcile_pose()` (lead by RTT/2, 8-unit dead zone, 35% blend under 150, snap beyond) now applies at keyframe match, delta dynamic records, and the remote host ship. (3) Glenn's tiny-TTL run went 20 min with NO kick — either the TURN_TTL secret wasn't in effect or Cloudflare enforces expiry lazily (docs say billing stops at expiry, disconnect "after a short delay"); the worker now `console.log`s `turn creds minted, ttl=Ns` so `wrangler tail` settles it. Verified: room + rejoin + impacts e2e green, worker tests green, selftest PASS.
- **2026-07-07 (aa)**: **RTT probe + latency-compensated prediction blend (Glenn: "heavy rubberbanding" on the client over a relay; "ok implement the ping and extrapolation"). PROTO_VERSION 10→11.** New MSG_PING(8)/MSG_PONG(9) on the UNRELIABLE channel (the reliable one would fold head-of-line stalls into the reading): each side pings 1 Hz with its own SDL_GetTicks, the peer echoes it verbatim, so only the sender's clock is read — no cross-machine clock math. Smoothed 80/20 into `net_rtt_ms_` (reset on rejoin; "net: rtt N ms (first pong)" log for drivers), shown on the debug overlay net line ("net: relay/relay 320ms"). The client's local-ship blend was correcting toward a pose ~RTT/2 stale — every correction dragged the ship backwards along its own trail, which IS the rubberbanding. Now the authoritative pose is extrapolated forward by RTT/2 (capped 250 ms) at its snapshot velocity before the correction is computed, a 12-unit dead zone leaves an in-agreement prediction untouched, and the 250-unit hard-snap backstop grows by speed×RTT so lag can't masquerade as a teleport (Glenn's "snapped 706 units regularly" was likely the respawn path — dead→alive transitions legitimately snap — but the log line now prints rtt+speed so the next report distinguishes the two). Verified: loopback selftest PASS, room + rejoin e2e green, both peers log a 3 ms first-pong on the local relay.
- **2026-07-06 (z)**: **ICE-path visibility + real TURN-expiry test rig (Glenn: show the connection type on the debug overlay; "lets check with a tiny ttl on a real connection").** `NetTransport::connection_info()` returns the selected pair's candidate types ("host/host" direct, "srflx/..." punched, "relay/..." TURN) — native via `rtcGetSelectedCandidatePair`, web via a 2 s `getStats` cache started on connect; the debug overlay (B) draws it as "net: ..." under fps, and both net ctors + the rejoin-Ready path NET_LOG "ice path ..." for driver assertions. `NEWTONIA_NET_FORCE_RELAY=1` sets `RTC_TRANSPORT_POLICY_RELAY` (native) so a loopback/LAN pair actually exercises TURN. Worker: `wrangler secret put TURN_TTL` overrides the credential TTL (clamped 30 s..48 h) for expiry testing. `test/e2e/turnexpiry.sh` ties it together: relay-forced pair on tiny-TTL creds → assert "ice path relay/relay" → wait out the expiry → assert the auto-pause/auto-rejoin self-repair reconnects (fresh creds ride the rejoin). CANNOT run in the dev container — no UDP egress (STUN probes time out) — so it targets any Linux box with real internet; the overlay line makes the manual mac equivalent trivial. Verified here: loopback pair logs "ice path host/prflx"/"host/host" and the overlay renders "NET HOST/PRFLX"; selftest PASS.
- **2026-07-06 (y)**: **Known errors headline the failure screen (Glenn).** LobbyFailed always said "SOMETHING WENT WRONG" with the real reason relegated to the 4 s status line. Known errors now take the headline — VERSION MISMATCH (status "UPDATE BOTH GAMES", shown 8 s via a new `set_status(text, show_ms)` duration), HOST REFUSED THE CONNECTION, COULD NOT CONNECT (firewall hint as status, 8 s), THE HOST IS NOT RESPONDING, NO ONE IS HOSTING THAT ROOM (own-room probe). Screenshot-verified via the fake old-host driver.
- **2026-07-06 (x)**: **Signaling-level version detection (Glenn's Windows↔old-mobile pairing showed the gap; root cause of that "same-LAN failure").** A PROTO mismatch only produced the honest "VERSION MISMATCH" message when the transports could connect (HELLO check); builds whose CONNECTION recipe diverged (pre-trickle, no TURN) never connected, so the player got a meaningless ICE timeout instead. Offer/answer signal frames now carry `"pv"` (the sender's PROTO_VERSION, as a string — `NetSig::offer_frame`/`answer_frame`, both backends): the joiner gates the offer BEFORE starting ICE (mismatch or absent → "VERSION MISMATCH - UPDATE BOTH GAMES" instantly), the lobby host discards mismatched answers ("PLAYER 2 HAS A DIFFERENT VERSION", room stays open), and the in-game rejoin path ignores them the same way. Old builds send no pv — absence identifies them. Worker: live frames were already forwarded whole, but the STORED offer (replayed to a late joiner — the common path) kept only the SDP, so `offer_pv` is now persisted/replayed/cleared alongside it. **DEPLOY ORDER MATTERS: `cd signal && npx wrangler deploy` BEFORE shipping any game build with this check** — the old production worker strips pv on the stored-offer replay, which would false-fail every normal pairing. The worker change is backward-compatible with old game builds (they ignore pv). Verified: reclaim/rate-key worker tests, fake pv-less old host vs real joiner → instant mismatch through the replay path, room + rejoin e2e green same-version, selftest PASS. Also (this hour): "PRESS FIRE FOR MENU" moved below the pause sub-lines (they now co-show since the (q) auto-pause), and the Android APK CI failure on 90c3899 was a runner-side SDK download flake (sdkmanager "ZipFile unknown archive" during setup, before compiling; same code green before/after — next push re-gates it).
- **2026-07-06 (w)**: **Deck: the picker hides under the native floating keyboard (Glenn's Deck session).** `code_entry_keyboard()` now returns whether `ShowFloatingGamepadTextInput` actually came up; `floating_kb_up_` hides the picker grid + button hints beneath it (the keyboard IS the input then — it types plain key events, so the standard typed-hint shows instead). Dismissal detection without Steam callbacks: the floating keyboard consumes controller input while showing, so the first real controller event that reaches the lobby proves it is gone and brings the picker back. **Milestone note from the same session: Glenn played a couple of levels cellular-iOS ↔ wifi-Steam-Deck and "it worked well"** — the first real cross-network cross-platform session (real NAT traversal + latency), which was the #1 pre-go-live blocker; it also serves as the Deck hardware pass for the lobby stick/code-entry work.
- **2026-07-06 (v)**: **Client teleport indicator snapped back to the triangle between keyframes (Glenn: circle only appears "after a tick"; "might be only updating on keyframes" — exactly right).** Delta records carry the `teleport_vulnerable` FLAG (state byte) but not `vulnerable_time_left`; the client's own `Asteroid::step` saw the timer at 0 on the next 8 ms step and cleared the flag straight back — so the circle only survived when a 1 Hz keyframe delivered the real timer (`restore_state` carries it). The delta apply's rising edge now starts the client's own 5000 ms window (the host relocation's constant); both clocks start within one snapshot interval and the host's clear still ships as a state-byte delta, keeping the window's end synced. Room e2e green.
- **2026-07-06 (u)**: **Controller X - PASTE on CodeEntry actually pastes the code (Glenn: "press X to paste does not work").** X called `start_paste()` — the MANUAL flow's SDP-blob path — which shrugs at a 5-letter room code. On CodeEntry, X now re-runs the clipboard CODE read as an explicit paste (`code_clip_explicit_`): clears any partial typing, reads the clipboard, and joins. Explicit paste is user intent like typing, so the own-room auto-join guards don't apply, and an empty/invalid clipboard answers "NO ROOM CODE ON THE CLIPBOARD" instead of silently doing nothing. Other screens keep X = manual-flow paste. Also confirmed with a fresh HUD screenshot: co-op players spawn with 3 lives remaining (the (t) charge), matching solo's first-spawn cost.
- **2026-07-06 (t)**: **Co-op scoring parity: the initial spawn costs a life (Glenn).** Every ship starts with `lives = 4` plus a free initial hull, so an online pair fielded 10 total ships to a solo run's 5 — co-op high scores weren't comparable. The net-host ctor now charges both players one life after setup (host-authoritative; the client mirrors lives via snapshots), so each co-op player fields exactly a solo run's ship count. Offline solo/local-2P unchanged. Room e2e green. Also this hour, Glenn confirmed on device/web: the client-side debris (o+p), the disconnect-pause/rejoin-sync flow (q), and the web picker fix (s) — #80 closed.
- **2026-07-06 (s)**: **Picker still showed on web (Glenn's local web build) — stray events counted as controllers.** The (n2) fix only guarded the `SDL_NumJoysticks` ctor seed; `NetLobby::controller()` ALSO set `controller_seen_ = true` on entry, and web_main.cpp's event loop forwards EVERY unhandled SDL event type through its `default:` case into `controller()` — any browser-side stray (TEXTINPUT, window/audio events) summoned the picker. The flag is now set only for real `SDL_CONTROLLERBUTTONDOWN`/`SDL_CONTROLLERAXISMOTION` events. Desktop no-controller CodeEntry screenshot confirms grid+hints absent (that run also carried stray events through the same path).
- **2026-07-06 (r)**: **Clipboard auto-join refused a live own-machine room (Glenn: mac host + mac client, first time doesn't auto-join — second time does).** The (#54) zombie-room guard blocks codes matching the persisted `last_hosted_code` pref — but both instances on one machine share the prefs INI, so a client launched AFTER the host loads the live room's code at startup and refuses it (launch order explains "works the second time": a client already running holds a stale pref snapshot, and any new room's fresh code no longer matches). The pref can't distinguish "sibling instance hosting right now" from "my kill-orphaned room in reclaim grace" — so the auto-join now PROBES pref-matched codes instead of refusing: it joins with an 8 s no-offer timeout (`own_room_probe_`). A live room answers in ~2 s and connects; an orphan fails fast with "THAT LOOKS LIKE YOUR OWN OLD ROOM" instead of the old 45 s hang. Codes hosted by THIS process (`s_last_hosted_code` — it closed its room on leaving) and confirmed-dead codes (`s_dead_code`) stay hard-blocked. Verified with a driver replicating the exact mac flow (host first → prefs+clipboard written → client launched after → JOIN, zero typing): auto-join probe connected, joiner bootstrapped.
- **2026-07-06 (q)**: **Disconnect/rejoin trio (Glenn's three asks).** (1) **Pause on P2 loss**: the host's run-once loss block now auto-pauses (`toggle_pause(false)`, no peer to broadcast to) instead of playing on solo — the pause key still resumes a solo session by hand; parking logic unchanged. (2) **Rejoin while paused syncs the world**: the paused tick never reaches the 10 Hz send, so the rejoin-Ready block pushes the keyframe immediately (`net_snapshot_timer_=100; net_host_send_snapshot(0)`) — the rejoiner's lobby bootstraps from it; the EV_PAUSE ships on the client's FIRST INPUT (the moment it provably exists — an event sent while it was still in the lobby would be dropped), putting both sides on the pause screen in sync. (3) **Safe-position the unparked hull**: an asteroid can drift onto the parked ship before the auto-pause lands, so the alive-unpark path now calls `safe_position(grid, try_current=true)` — stays put when clear, relocates when overlapped; the rejoiner adopts the pose from its bootstrap keyframe, so no warp-blend concern (the dead-unpark path already ran safe_position via respawn). rejoin.sh e2e extended: asserts the "paused awaiting rejoin" marker and resumes from the host post-rejoin (P) with both sides alive — green, joiner bootstrap confirmed from the paused keyframe.
- **2026-07-06 (p)**: **Client debris froze at the impact site (Glenn: "something is drawn at the impact site and that's it").** Root cause was not the (o) spawn but a wipe: on the client, `Ship::restore_state` runs `respawn()` → `reset()` inside EVERY 10 Hz snapshot apply, and `reset()` cleared `debris` — so any spray (the (o) cosmetic impacts AND the EV_SHIP_IMPACT bounces) lived under 100 ms: a static flash, no visible flight. The projectile vectors reset() clears are refilled by the snapshot extras immediately after; debris has no such source. `reset()` now skips the debris clear when `net_quiet_respawn` is set (true for the whole client-game lifetime — the same flag that already marks these as snapshot-driven respawns), so client debris lives its natural ≤1.5 s and flies with its spawn velocity, matching the host. Host/solo behaviour unchanged (flag is false there). Spin-and-fire e2e still green (5 cosmetic impacts, zero errors).
- **2026-07-06 (o)**: **Bullet-vs-asteroid impact debris is now client-side cosmetic — PROTO 10 (Glenn: invincible-asteroid debris missing on client; "make them client-side-only to reduce network usage").** The old path replicated impacts as EV_ROID_THUD/TING events, but the client's handler only sparked the ASTEROID (`net_spark_asteroid_at` → `explode()`), never the bullet-debris spray the host shows (`Ship::explode` at the impact point) — hence Glenn's report. Instead of adding more event traffic, the client now detects these contacts itself: `Ship::net_cosmetic_impacts(grid)` runs per step in `tick_net_client` over both players' replicated bullets, grid-probes for asteroid contact, and for asteroids a plain bullet cannot kill (invincible/tough>1 → thud; reflective/phased/armoured-shielded-arc → ting; armour arc uses the host's ±120° `shield_dot` test sans entry-trace) sprays the ship's own debris at the bullet and plays the cue under the same 125 ms rate limit. Killable asteroids are skipped — their death explosion already replicates via removal. A per-bullet `net_sparked` flag (transient, never serialized) stops a replicated copy strobing while it crosses the asteroid between snapshot corrections. The entire `Asteroid::net_impacts` static outbox is deleted (asteroid.cpp pushes, the armour-deflection push in ship.cpp, the host drain, all clears) — EV_ROID_THUD survives only for the gen-20 station-hull deflection, EV_ROID_TING is retired. PROTO 9→10 (a mixed pairing would double or drop the cues). Verified: room e2e green, plus a targeted spin-and-fire e2e at gen 3 — 4 "cosmetic impact thud" markers on the joiner, zero errors.
- **2026-07-06 (n2)**: **Web: picker grid showed with no controller (Glenn's itch/web test).** The (j) `controller_seen_` seed reads `SDL_NumJoysticks() > 0` at lobby open — but browsers report phantom gamepad slots (Chrome's `navigator.getGamepads()` returns four nulls with nothing connected), so Emscripten's SDL over-counts and the CodeEntry picker rendered for keyboard-only web players. The seed is now `#ifndef __EMSCRIPTEN__`; on web the picker appears on the first real controller event instead (mobile browsers are touch mode and never draw it regardless). Desktop/Deck behaviour unchanged. web.yml is the compile gate (no emcc in the dev container).
- **2026-07-06 (m)**: **Lobby stick nav hysteresis (Glenn: "the stick felt a little sensitive on a gamepad on mac").** The (j) stick handlers armed at a plain ±8000 edge (~24% deflection) — light nudges moved the selection, and a loose stick hovering near the line could re-trigger on wobble. Each stick direction now arms at `STICK_ON` 16000 (half deflection) and only re-arms after falling back under `STICK_OFF` 8000 (classic hysteresis). The right-trigger confirm keeps the plain 8000 edge on purpose — triggers spring fully back to zero, so they don't hover, and it stays consistent with the menu's RT feel. The menu's own LEFTY nav still uses the plain edge; if it feels twitchy too the same two-threshold pattern drops in.
- **2026-07-06 (l)**: **CodeEntry hoisted above the Deck's floating keyboard (Glenn: OSK covered the bottom of the code characters).** The desktop CodeEntry stack sat mid-screen (slots at virtual +20 → glyphs to -76) and the Deck keyboard docks over the bottom half — same failure the touch layout already solved for the iOS soft keyboard, same fix: heading now at +200 directly under the title, code slots at +120 (glyphs to +24, entirely in the top half), hint line at -20, picker rows up to -130/-182 (still clear of the -320 status line; the grid may sit under the OSK, which is fine — with the keyboard up, the keyboard is the input). The floating-keyboard field rect follows the new slot position. Verified by screenshot; STEAM_BUILD stub syntax-check + room-connect e2e clean.
- **2026-07-06 (k)**: **Steam Deck: auto-summon the floating keyboard on CodeEntry.** The Deck has an OSK all along (STEAM+X types plain key events our keyboard() path already accepts — (j)'s report was really a discoverability failure), and Steamworks can summon it: `steam_build.h` gained `steam_show_floating_keyboard(x,y,w,h)` / `steam_dismiss_floating_keyboard()` wrappers around `ISteamUtils::ShowFloatingGamepadTextInput` (single-line mode; delivers ordinary key events, no read-back API needed) — no-ops off `STEAM_BUILD`, false on hardware without a floating keyboard (plain desktop), so callers just leave their own entry UI up. `code_entry_keyboard()` now drives it on non-touch at the same open/dismiss edges the touch soft keyboard uses (enter CodeEntry / expired-room bounce-back → show, positioned clear of the code slots' pixel rect; submit / leave → dismiss). The (j) controller picker stays underneath as the universal fallback (non-Steam builds, desktop pads, the future Xbox port). Deploy-time-compile risk handled two ways: signatures verified verbatim against Valve's docs + the current SDK's isteamutils.h (`SteamUtils010` has both methods — any SDK from ~1.55 on), and the `STEAM_BUILD` paths of net_lobby.cpp/menu.cpp syntax-checked locally against a stub header carrying those exact signatures. Loopback selftest + room-connect e2e clean. Needs a Deck to see the keyboard actually pop.
- **2026-07-06 (j)**: **Steam Deck lobby: analog stick + controller room-code entry (Glenn's device reports).** `NetLobby::controller()` only handled `SDL_CONTROLLERBUTTONDOWN`, so the Deck's stick did nothing on HOST/JOIN — added `SDL_CONTROLLERAXISMOTION` with the menu's ±8000 edge-detect pattern (LEFTY moves the Choose selection, LEFTX/LEFTY walk the picker, right trigger mirrors A since it's the in-game fire button). Room-code entry had no path at all without touch or a soft keyboard: CodeEntry now shows a character-picker grid — the code alphabet in two centred rows under the slots, selected cell enlarged, d-pad/stick moves (wrapping; the short second row clamps), A/RT types through the existing `code_entry_key()` (so the join still auto-fires when the fifth slot fills, and the alphabet stays the single gate), B deletes and only backs out once the field is empty (BACK always exits), X pastes as before. The grid + "A - TYPE B - DELETE X - PASTE" hint line appear once a controller is seen (`SDL_NumJoysticks()` at lobby open — always true on a Deck — or the first controller event); keyboard typing works alongside, and the touch layout is untouched (a paired controller on touch keeps the old A=confirm/B=exit so it can't type from a picker that isn't drawn). The alphabet itself is now single-sourced: `NET_ROOM_CODE_ALPHABET` exported from net_signal.cpp (matching worker.js's `CODE_ALPHABET`), with `net_room_code_char_ok()` reduced to a `strchr` over it — the picker, the validator, and the docs can't drift. Verified: picker wrap arithmetic unit-checked (5 cases + stress), screenshots of the grid layout (no collision with the status line at -320 or the exit band), room e2e keyboard flow still connects clean, loopback selftest PASS. Controller navigation itself needs the Deck — pattern is the menu's proven one.
- **2026-07-06 (i)**: **TapBand refactor (review): one definition yields draw + hit rect.** New `view/tap_band.h/cpp`: a `TapBand` holds the Typer anchor (normalized x, virtual y, glyph size) plus finger padding, optional to-edge extension, and a normalized horizontal strip; `draw()` puts the label on the anchor and `contains()` hit-tests the same numbers — text and tap zone can no longer drift apart (the failure mode behind three playtest bugs: the misaligned share band, the CodeEntry exit, the game-over dead-end). Converted pairs, each verified arithmetically identical to the old hand-derived checks: the shared bottom exit strip is now `TapBand::return_to_menu` (drawn by the overlay and the lobby, hit-tested by `GLGame::touch_tap` and `NetLobby::touch_tap` — previously the -420/-370 pair lived in four places), the lobby's CodeEntry BACK (`kBackBand`, was `vy>=430 && nx>=0.72` vs a draw at 0.7·W/480) and RoomHost share line (`kShareBand`, replacing the `share_line_y_` record-at-draw member), and the friendly-fire toggle became `GLGame::ff_toggle_band()` — computed from the live viewport height instead of the hit-test's hard-coded `-500` with its "vhb+55 = -545" derivation comment (now also correct at non-600 virtual heights). menu.cpp's row band kept its slot-mapping design but the duplicated gap formula moved into one `menu_row_gap()` used by both `draw_menu_rows` and `menu_row_at`. `tap_band.cpp` added to the iOS pbxproj (AA/AB000068); every other build globs. Verified: netplay build + loopback selftest, room e2e (menu → lobby → connect → play, zero errors), lobby screenshots confirm identical desktop rendering.
- **2026-07-06 (h)**: **Host signal-upkeep dedup (review refactor).** `net_host_signal_maintain` and `net_host_rejoin_poll` each carried their own copy of the M3-1 reclaim countdown and the Ice/Closed/Error signal-event handling — the copies had already diverged once (review bug 6's rate-limited fix had to be applied twice). Extracted two private helpers both loops call: `net_host_signal_reclaim_tick(delta)` (the countdown → `connect_host_reclaim`) and `net_host_signal_common_event(ev)` returning Unhandled/Handled/Dropped (Dropped = `net_signal_` deleted, caller must return). The rejoin-only Ice extra (`net_rehost_->set_ice_servers`) folded into the common handler behind an `if (net_rehost_)` — always null on the healthy path, so no behaviour change there. Each loop now handles only its own events: maintain keeps Room-log + PeerJoin fast-loss-detect, rejoin keeps Answer/Cand/Room-reoffer. glgame.h includes net_signal.h (lightweight, `<string>` only) since the helper signature names `NetSignal::Event`. Verified: netplay build + loopback selftest PASS, full rejoin e2e (SIGKILL joiner mid-game → "player 2 lost - room reopened" → relaunch + same code → "player 2 rejoined", play continues, zero error lines).
- **2026-07-06 (g)**: **Per-apply / per-delta allocation churn cut (review options A+B; C deferred to benchmarking).** Host delta path: `build_save_data(false)` skips capturing all asteroids into a list the delta immediately discarded (~60 KB of capture_state copies, 9x/s, gone); the removed-asteroid detection is now O(N) via a `present_ids` set recorded in the existing single object pass (was a nested "for each known id, scan all objects" O(N^2)). Client apply path (10x/s): the id→Asteroid lookup is a reserved `std::unordered_map` instead of a fresh `std::map` (no per-node tree inserts); pickups skip the delete/new storm when the set is unchanged (same count + matching stationary positions — reconciled by the 1 Hz keyframe on the astronomically-unlikely same-spot type swap); `Ship::restore_state` gained a fast path — when the weapon roster matches (same counts, same primary indices, same secondary kinds, no timed GodMode) it updates ammo/selection in place instead of deleting and re-newing the whole arsenal (the common case, since only ammo/selection change between snapshots). All behaviour-preserving. Verified: loopback selftest + room e2e (connect, 3 level skips, both players firing 8s so the weapon fast path runs every tick, asteroid deltas + warps applied on the joiner, no crashes/bad_alloc/decode errors). Option C (object pooling for weapons/pickups/enemies) intentionally left for a real-device benchmark — the remaining churn is bounded and B removed the dominant sources.
- **2026-07-06 (f)**: **Code-quality review of the whole feature (8 finder angles) → 8 bugs fixed (PROTO 8→9).** Correctness cluster, all remote-reachable: (1) `deserialize_game`/`net_apply_keyframe_asteroid_ids` resized vectors from unchecked wire uint32 BEFORE `net_state_sane` ran — a hostile count forced a multi-GB alloc → terminate; added `read_count(max)` bounded reads (caps = net_state_sane limits) and reordered the id size-check before its alloc. (2) The static host event outboxes (`Ship::net_shots/net_booms/net_ship_impacts`, `Asteroid::net_impacts`) held raw `Ship*` across GLGame instances — client→menu→host in one process drained freed pointers (UAF); now cleared in both GLGame ctors via `net_clear_event_outboxes()`. (3) `controller_added` bound a client's pad to the host's ghost ship (`players->front()`); now binds only `local_player()` when online. (4) NaN world dims passed `net_state_sane` (all NaN comparisons false) → Grid/WrappedPoint UB; added `!isfinite` guard. (5) Station enemies resurrected as zombies for ~2s on the client (`GLStation::capture_state` serialized dead-but-fading enemies, `restore_state` forced `alive=true`, applied 10x/s) — now skips `!is_alive()` enemies (death boom already replicates via EV_WORLD_BOOM). (6) A worker "rate-limited" reclaim was treated as permanent room loss on the HOST (deleted the signal) — the joiner already retried; both host handlers now back off 15 s and keep the room. (7) `pack_pos` int16 overflowed once the world grew past 32k (~gen 30, +3000/gen from gen 20) → wrong-place sparks/audio; repacked as uint16 world-fractions (always fits, sub-unit resolution; PROTO bump forces the mismatch). (8) `~NetLobby` leaked its `GLStarfield` (heap + GPU VBOs) every ONLINE visit; now deleted. Offline regressions from the audit: #10 (scoring vs invincible targets) is as-designed per Glenn; #9 (game-over save skipped on disconnect exit) is moot — no save files online. Verified: loopback NET SELFTEST PASS + room-flow e2e (PROTO 9 pairs, joiner bootstraps the keyframe through the bounded reader, both alive, no aborts/bad_alloc). Remaining review items tracked as tasks: TapBand geometry refactor, NET_LOG gating, per-apply allocation churn (options first), signal-upkeep dedup, HUD→overlay.cpp, net_spark_asteroid_at→Grid, worker per-candidate storage.
- **2026-07-06 (e)**: **Rejoin wait made honest (Glenn killed the mobile host and watched the client "go back to the join and automatically try to rejoin").** Auto-rejoin STAYS (it's the seamless recovery for host blips/backgrounding — the client can't distinguish those from a kill, and the room sits in reclaim grace either way), but the wait now reads as what it is: in rejoin mode the RoomJoining screen shows "WAITING FOR THE HOST TO COME BACK" with a live "GIVING UP IN n" countdown instead of "JOINING THE ROOM"; the budget drops 90 s → 60 s AND ticks continuously — the e2e proved this was the real bug: a rejoin that successfully JOINS the grace-held hostless room schedules no retries, and the old budget only ticked between retries, so that wait was UNBOUNDED (Glenn's "ages" was the relay's grace expiry doing the rescuing); expiry lands on "THE HOST DID NOT COME BACK / COULD NOT RECONNECT" instead of a generic fail. The retry-fire logic is restructured to only fire on a scheduled countdown crossing zero (never mid-attempt). The CANCEL and RETURN TO MENU touch bands were verified already present in rejoin mode — the wait was always escapable, just not labelled. Discussed and chosen with Glenn over the alternative of an explicit reconnect/leave choice card (rejected: it would cost the zero-touch blip recovery). Also: "THE HOST LEFT THE GAME" card moved up (y=60 → 160) clear of the "Paused" overlay at y=30 — both show when the host leaves a paused game (Glenn's screenshot-level catch).
- **2026-07-06 (d)**: **Friendly fire becomes a synced room rule (PROTO 7→8 — both peers rebuild) + host-BYE stops the client's self-rejoin loop (Glenn's iOS reports).** (1) Friendly fire: gameplay was always host-authoritative (damage runs in the host sim, the G toggle is behind `host_keys`), but the client's HUD showed its OWN preference — new `EV_FRIENDLY_FIRE` (arg 0/1) announces the host's rule at client bootstrap (join + rejoin, sent from the NetHost ctor and the session-Ready site) and on every toggle; the client adopts it into the GLGame copy only (g_prefs untouched) and flashes a FRIENDLY FIRE ON/OFF banner when it changes. The toggle is factored into `host_toggle_friendly_fire()`, also reachable on touch: the "friendly fire on/off" HUD text (two-player only, vy≈-545) is now a tap region (centre third — the OSD shoot/mine circles reach into the bottom strip on the right), and the OFF state draws on touch so the region stays findable. Verified e2e: joiner logged `net: friendly fire off` at bootstrap then `on` after the host's G. (2) Host BYE (Glenn: kicked to the join screen, auto-joined the dead room from the clipboard, "waited for ages"): EV_BYE set the same `net_connection_lost_` as a real drop, so M3-1's auto-rejoin walked back into a room whose host said goodbye — and if the host's signal socket was dead at quit (no `{t:"close"}`), the relay holds the room in reclaim grace and ADMITS the join to wait forever. Now a client receiving BYE clears the rejoin code (card: "THE HOST LEFT THE GAME" / TAP FIRE FOR MENU), and `NetLobby::mark_room_dead()` makes the clipboard auto-join refuse that code for the rest of the run — also set on fatal relay answers (host-closed/no-such-room/expired, not rate-limited). Verified e2e: host Esc-quits mid-game → joiner logs `net: host bye - no rejoin`, zero auto-rejoin lines. Also: Glenn re-reported the CodeEntry no-exit on 1.41.0 — the (c5) CANCEL band landed after that tag; ships in the next one. **Device-confirmed by Glenn on netplay-v1.41.4 (iOS): friendly-fire toggle works, "THE HOST LEFT THE GAME" works, no auto-join to own server.**
- **2026-07-06 (c)**: **iOS device-playtest batch (Glenn, TestFlight netplay-v1.40.0) — four linked reports, one root story: no in-game exit on iOS.** (1) **Pause screen RETURN TO MENU band** (touch): mid-game iOS had NO path to the menu (gk.menu is Esc, nothing touch sends it; Android has hardware back). `GLGame::touch_tap` now honours the bottom band when `!running` — save_progress() + Menu, mirroring the Esc path; the overlay labels it. (2) **Online game-over dead-end**: the game-over band required ALL players out, so a dead local ship spectating a live/parked peer had no exit — band + tap now also fire when `net_active()` and the LOCAL player is fully out. (3) **Zombie self-join after app kill**: with no exit path Glenn killed the app; relaunch lost the in-memory `s_last_hosted_code` guard and the clipboard auto-join walked him into his own dead room until the 45 s backstop. The last hosted code now persists (`last_hosted_code` in preferences, written when the room frame lands) and the auto-join guard checks both. (4) **iOS keyboard dropping letters in code entry** — the exact class of bug the Android (v) fix predicted ("iOS keeps the TEXTINPUT path... untested on hardware"): switched iOS to the Android-proven scheme — KEYDOWN is the single source of typed characters (SDL's insertText synthesizes per-char key events; TEXTINPUT not forwarded, suppressions removed), plus Android's finger_down guard while text input is active ('\r'/'x'/'n' taps were leaking into the code field; X and N are code-alphabet letters). Desktop/Android behaviour unchanged; needs a fresh device build to verify. (5, follow-up report) **CodeEntry had no reachable exit on iOS**: the RETURN TO MENU band sits at y=-420, physically under the soft keyboard, and the keyboard can't be dismissed — a CANCEL band now sits in the TOP strip (y=480, tap zone vy>=430) on touch, wired to `leave_to_menu()` (which already closes the keyboard and, when hosting, the room).
- **2026-07-06 (b)**: **`netplay-v*` tag namespace for netplay test releases (Glenn: tag netplay releases without touching the normal pipeline).** Standard prefix-namespace approach — a semver prerelease suffix (v1.2.3-netplay.1) would still MATCH the `v*.*.*` Actions glob (`*` matches dots), so a distinct prefix is the only clean discriminator. All four deploy workflows now also trigger on `netplay-v*` and route to test channels: Steam → `netplay` branch (v-tags keep `beta`; the dispatch default stays `netplay`), itch → new `html5-netplay` channel (public `html5` untouched), TestFlight/Play-internal are test tracks either way (version parsers strip the `netplay-` prefix so netplay-v0.9.1 becomes 0.9.1). Usage: `git tag netplay-v0.9.0 && git push --tags`. Also this session: iOS parity-gate fix rounds — absolute NETLIBS paths (xcodebuild links from the project dir; same latent bug fixed in deploy-ios), then CoreBluetooth undefined symbols, resolved per Glenn ("no bluetooth") by building the sim SDL2 with `-DSDL_HIDAPI=OFF` like the device build and dropping `-framework CoreBluetooth` from the hand link too — SDL's BLE hidapi was the only referent, and the deployed app never linked it.
- **2026-07-06 (a)**: **M3-5 — all deploy actions now build netplay (Glenn: "time to make the deploy actions work with netplay").** Audit first: deploy-itch (`make web`) and deploy-android (root CMakeLists, `NEWTONIA_NET` default ON) already carried it; deploy-ios and all three deploy-steam builds did not. deploy-ios: the xcodeproj had NO net_*.cpp sources at all (added, IDs AA/AB0000 60-67) and no netplay settings — new `$(NEWTONIA_NET_DEFINE)` / `$(NEWTONIA_NET_HEADER_PATH)` entries expand empty for plain Xcode builds and are fed by the workflow, which now builds MbedTLS+libdatachannel static for iphoneos/arm64 (ios.yml's sim recipe with the sysroot swapped) and appends the archives to OTHER_LDFLAGS. deploy-steam: Windows goes MinGW-static libdatachannel with msys2's OpenSSL (no MbedTLS/UCFG dance needed — no universal-binary or MSVC constraint, and wss verification is upstream-disabled on `_WIN32` either way (n)) with `-DRTC_STATIC` (rtc.h would otherwise declare dllimport) and `--start-group` over the build tree's archives + `-lssl -lcrypto -lws2_32 -liphlpapi -lbcrypt -lcrypt32`; macOS mirrors macos-dev's proven `--universal` recipe with the fat dylib cp'd into Frameworks and codesigned explicitly (the `bundle()` otool walk skips `@rpath` deps — same reason libsteam_api is manual); Linux runs `build_netplay_deps.sh` INSIDE the steamrt sniper container (links the runtime's own OpenSSL; `libssl-dev` added to its apt list) and stages the .so under the exact SONAME the binary NEEDs (objdump) next to the `$ORIGIN`-rpath'd binary. Since deploys only fire on tags/dispatch, the recipes are proven in dev CI on every push: windows.yml + linux.yml gained the same builds plus the headless loopback selftest (glut.cpp's hook runs before any window/GL — verified locally on Linux this session: NET SELFTEST PASS), and ios.yml gained an xcodebuild compile gate of the real project with the netplay vars (the hand-rolled clang build never exercised the pbxproj, so project regressions were invisible until deploy time). Deploy jobs each run the selftest too, as a last-line gate.
- **2026-07-05 (gg)**: **Gen-20 station: client hang fixed + enemy bullets replicate (Glenn hit it: "behaves REALLY badly once the enemy ships arrive... slow and hung", screenshot showed chains of accumulated enemies).** Root cause: `GLStation::restore_state` APPENDS a freshly-built `GLEnemy` per record — correct save-load semantics, catastrophic at the client's 10 Hz apply rate: hundreds of ships piled up until the frame collapsed. Fixes: (1) the client wholesale-rebuilds — all enemies are station-owned, so `net_apply_state` deletes the enemies list before every station restore (and when the station goes absent); (2) enemy bullets ride a new extras section (**PROTO 6→7**, in enemies-list order — the rebuild recreates them in the same order every apply), so their fire is visible on the client like the mini-station's; (3) kinematic client extrapolation between applies: `GLStation::net_client_step` (drift + ring spins) and Object-level stepping + bullet flight for each enemy — no AI, no firing, host owns those. Verified: gen-22 e2e — 22 level skips, 40 s of play with the station deploying waves, both processes alive, client tracked 23 world transitions, deltas ~32 KB/s at 294 asteroids, 43 impact events. Enemy GLEnemy churn is 10 Hz new/delete of a handful of objects — acceptable; index-matched reuse is the optimization if it ever shows.
- **2026-07-05 (ff)**: **Mini-station bullets replicate + its motion smooths (Glenn confirmed the (ee) suspicion: invisible client-side bullets).** The Save record carries no bullets and the client never stepped its copy, so its shots were invisible AND the station itself teleported at the 10 Hz apply rate (unlike asteroids, which the client extrapolates). Fix: a mini-station bullet section rides both keyframe and delta payloads after the per-ship extras (**PROTO 5→6**; both peers rebuild), applied after the state so host presence is settled; and a new `GLMiniStation::net_client_step` (drift + ring spins + bullet flight — no firing, no collisions) runs in the client step loop, so restores only correct tiny error. Verified: gen-11 e2e — joiner tracked 14 world transitions with the station live and firing (v6 extras parsing throughout), 31 impact events, deltas steady at ~19 KB/s, no decode errors. GLStation (gen 20) will need the same treatment for its deployed enemies' bullets when testing reaches it — noted, not yet done.
- **2026-07-05 (ee)**: **Station/enemy sounds attenuated + relayed (closes the audit's last queued sound item).** Host side: enemies' `sound_volume_scale` goes net-aware (0.5 x distance-to-LOCAL-player online — the old `is_visible_to_any_player` gate counted the REMOTE player's view, playing full-volume shots from someone else's dogfight); the mini-station's shot gains the same per-tick scale (solo now attenuates by nearest player too — it used to fire at full volume from anywhere); the mini-station destruction boom attenuates likewise. Client side, three new positional events: **EV_WORLD_SHOT** (enemy/mini-station gunfire — enemies already pass through `fire_bullet_from_gun`'s outbox, the mini-station pushes explicitly), **EV_WORLD_BOOM** (non-player ship deaths via a new `Ship::net_booms` outbox from `kill()`; player deaths still arrive via extras — not relayed, no doubling), **EV_STATION_BOOM** (mini-station destruction), all played at `net_listener_volume` of the packed position, borrowing per-instance chunks (every play site now sets chunk volume first, including `Ship::kill`'s explode). Station-hull bullet deflections reuse EV_ROID_THUD with the bullet position. **Known gap, needs a look when Glenn reaches gen >=10 online: mini-station/enemy BULLETS may not replicate to the client at all** (Save::MiniStation carries no bullets; the client doesn't step its copy) — flagged for verification, would be its own fix.
- **2026-07-05 (dd)**: **Web link fix + join-flow UX (Glenn's `make web` + flow feedback).** The share-seam no-op stubs sat inside net_transport.cpp's `#ifndef __EMSCRIPTEN__` block, so wasm-ld had no `net_share_available`/`net_share_text` — hoisted above the guard (web correctly gets the clipboard wording; `make web` on the Mac is currently the only pre-master Emscripten link check, worth running when net seams change). TRY AGAIN after a failed join now lands on an EMPTY join screen with a fresh transport (`retry_join()`; failed host attempts still return to the chooser; the clipboard auto-join is deliberately not re-armed — that code just failed; `reset_to_choose` also clears rejoin mode: a manual retry is a fresh join). New CANCEL band (touch) on all three joiner waiting screens (JOINING THE ROOM / FOUND THE HOST / CONNECTED) → same retry_join path, abandonable at any time.
- **2026-07-05 (cc)**: **Remote gunfire, distance-attenuated (Glenn's call), both directions.** Discovery: `Ship::sound_volume_scale` already exists as the per-ship attenuation knob (enemy AI uses it). New `GLGame::net_listener_volume(p)` — same falloff as `sound_volume_for_point` but with the LOCAL player as the only listener (the split-screen variant treats every player as a listener, so a remote ship is at distance zero from its own sounds). Host: sets the remote ship's `sound_volume_scale` per tick — P2's gun (previously FULL volume on the host, split-screen legacy) now fades with distance. Client: `fire_bullet_from_gun` queues shots into a `Ship::net_shots` outbox; the host relays its own player's via **EV_REMOTE_SHOT** and the client plays P1's per-instance shoot chunk at listener volume (its own shots are local; the host simulates the client's weapon, so only P1's travel). The client also sets the remote ship's scale per tick, so its self-played sounds that DO run client-side (death explosion, god-mode tics) attenuate too. Room-flow e2e green. Task 43's leftovers are now just station/mini-station/enemy sounds (gen>=10/20 content).
- **2026-07-05 (bb)**: **Full effects-replication audit (Glenn: "search for anything else we missed") + the findings, implemented.** Root insight for the missing visuals: `CompositeObject::explode()` fills the object's own `debris` vector, which is NEVER serialized — impact particle sprays were invisible on the client for ships and asteroids alike. Batch: EV_ROID_THUD/TING now carry the impact position (packed int16 pair) and the client sparks the nearest asteroid (`net_spark_asteroid_at`, ≤80 units) plus plays the sound; armoured-face bullet deflection (which never reaches `Asteroid::kill()`) queues its ting too; non-fatal ship-vs-asteroid bounces ride a new `Ship::net_ship_impacts` outbox → **EV_SHIP_IMPACT** (player idx + armour-ting bit; client debris-sprays that ship); asteroid-vs-reflective bounces → **EV_ROID_BOUNCE** (arg = host-computed volume); mines/giga-mines that vanish from the snapshot play their explosions client-side (proximity-diffed, suppressed on generation rollover via `net_world_rebuilt_last_apply_` — a rebuild is not a barrage); replicated missiles adopt/share the fly-loop via `sound_handle` (halts when the last holder dies); teleporting-asteroid relocation plays warp + arrival debris on the `teleport_vulnerable` false→true transition (no event needed); a newly-appearing nova shockwave plays its boom (`net_nova_arrived`). Also: GAME OVER on touch gets a RETURN TO MENU bottom tap band (GLGame::touch_tap, same geometry as the lobby's; 3 s grace as with keys); deliberate host shutdown now reads **"SERVER SHUT DOWN" / "THE HOST ENDED THE GAME"** (worker remembers `closed` per code and replies err "host-closed" until a new host claims it — **worker redeploy**, same pending one); "net: roid impact" prints are back but gated behind NEWTONIA_NET_DEBUG=1 for the e2e drivers. Verified: reclaim_test 24/24 (host-closed reasons), thud e2e 8/8 positional impacts, host-quit e2e, room-flow regression. Still queued (task list): station/mini-station/enemy sounds (gen>=10/20), remote player's gunfire parity.
- **2026-07-05 (aa)**: **More client presentation gaps (Glenn's device round): countdown tics, pickup sound, shield-hum blips, missiles.** New events EV_LEVEL_TIC (host's 1/s level-clear countdown tick) and EV_PICKUP (collection cue) — additive codes, both peers rebuild as usual. Shield-hum blips root-caused for real this time: `Ship::respawn` runs inside EVERY 10 Hz snapshot restore and start-then-halts the hum — audible blips escaped between restore and the extras' mute; new `Ship::net_quiet_respawn` (set for client games) makes the snapshot extras the ONLY hum authority on a client. Missiles: the wire doesn't carry `thrust` or the trail, and the 10 Hz wholesale replace reset both (teleporty look, restarting trails) — replicated missiles now adopt trail+thrust from the nearest previous missile (≤150 units), and a missile that vanishes with >300 ms life left exploded on the host → `Ship::net_missile_exploded` plays debris+sound client-side (expiry still detonates locally; no doubling either way). The in-flight fly-loop hum is still host-only (per-missile sound-handle system; noted, not scheduled). Typer skips apostrophes (no glyph). Auto-join self-host guard + TAP TO TRY AGAIN landed in the previous commit. "net: roid impact" debug prints removed at Glenn's request. Room-flow e2e green. Still open: client's own ship-vs-asteroid impact effect (new report), missile fly-loop.
- **2026-07-05 (z)**: **Impact events verified working + self-hosted-code auto-join guard.** Glenn reported thud/ting still silent on the client — e2e (host skips to gen 5, sprays into invincible/reflective rocks) proved the (y) events DO arrive and play: first run showed 0 client events purely because the diagnostic printfs weren't flushed and SIGTERM drops stdio buffers; with fflush, 6/6 impacts logged client-side. So the report almost certainly means one side ran a pre-(y) build — the HOST is the sender and both peers must be current. Diagnostics kept ("net: impact ev out"/"net: roid impact"). Also, per Glenn's host→quit→join flow: the JOIN screen's clipboard auto-join now refuses the code THIS instance hosted (`s_last_hosted_code`, in-memory) — the host auto-copies its own code, so after quitting, auto-join walked the player into their own dead room (typing it manually still works); LobbyFailed's hint is "TAP TO TRY AGAIN" on touch (ENTER is a desktop cue); and the "hangs for ages" part of that flow is the (w) zombie-room fix — the instant-close half lives in the WORKER, so it needs the redeploy; the 45 s client backstop covers old relays.
- **2026-07-05 (y)**: **Client-side impact/death audio (Glenn: silent asteroid destruction, silent invincible/reflective hits) + touch menu spacing.** The explosion sound lives in `add_children()` (host-only online) and the thud/ting impact sounds play inside `Asteroid::kill()` during collisions the client never simulates. Fixes: `Asteroid::play_explode_sound()` (the clamped player factored out of add_children) now also fires on the client's two removal paths (keyframe id-diff and delta removals) for genuinely-dead asteroids; and kill() queues each impact sound it plays into a static `Asteroid::net_impacts` outbox the net host drains into new **EV_ROID_THUD / EV_ROID_TING** events (additive event codes, no proto bump) — the client plays them straight, host's 125 ms rate-limit inherited. The queue is cleared every tick in every mode (client's own ghost-removal kills are dropped). Also: touch main-menu rows now spread over a taller band (160 → -300; they're finger targets) with the high-score block moved down. Room-flow e2e green.
- **2026-07-05 (x)**: **Android: ALL in-game touch input dead (Glenn, hosting).** SDL2 starts with text input ACTIVE by default — `SDL_IsTextInputActive()` is true from init until the first `SDL_StopTextInput()`. The (v) finger_down guard ("no game-key synthesis while the soft keyboard is up") was therefore permanently on for any session that never opened the code-entry keyboard: joystick and buttons dead in-game, while menus still worked (touch_tap bypasses the guard). Canonical fix: `SDL_StopTextInput()` once at startup in android_main and ios_main (whose key-event guards had the same latent bug); only the lobby's code entry re-enables it.
- **2026-07-05 (w)**: **Zombie room on deliberate host quit (Glenn: ex-host rejoining their own code sat on "JOINING THE ROOM" forever).** A deliberate host leave was indistinguishable from a crash, so the relay granted the 2-minute reclaim grace and admitted joiners to wait for a host that had told no one it wasn't coming back — and the host's own code was still on their clipboard, so the auto-join walked them straight into their own dead room. Fix, three layers: (1) new `{t:"close"}` signal frame — `NetSignal::send_close()` on both backends — sent on deliberate host teardown (lobby `leave_to_menu` while hosting, and the host `GLGame` destructor, i.e. quit-to-menu/game-over; that destructor's "closes the room" comment had been wrong since M3-1 introduced grace); (2) the worker `expire()`s the room on close — waiting joiners get `err expired`, the code immediately rejects joins with `no-such-room`, crashes still get the full grace window (they send nothing); (3) joiner-side backstop — 45 s on RoomJoining with no offer fails to "THE HOST IS NOT RESPONDING" instead of hanging (rejoin mode exempt: hostless-grace waits there are legitimate, bounded by its own 90 s budget). **Worker must be redeployed for (2).** Verified: reclaim_test.js grew to 24 checks (close → joiner told + join rejected, no grace); real-binary e2e (host mints room, Esc-quits, raw WS join gets instant no-such-room); normal room-flow e2e regression green.
- **2026-07-05 (v)**: **Android soft keyboard dropping ~3 of 4 letters — root-caused in SDL's Java layer.** Soft keyboards deliver text to SDL two ways: `commitText` (SDL synthesizes KEYDOWN/KEYUP per char *and* sends SDL_TEXTINPUT) and real IME KeyEvents (KEYDOWN/KEYUP only, **no TEXTINPUT** — `SDLInputConnection.sendKeyEvent` passes them through). GBoard mixes both, so the (t) fix — feed code entry from TEXTINPUT, drop printable key events while text input is active — silently discarded every KeyEvent-delivered letter. New scheme: **KEYDOWN is the single source of typed characters on Android** (fires exactly once per char on every delivery path, covers Bluetooth keyboards); TEXTINPUT is not forwarded at all; and `finger_down` synthesizes NO game keys while text input is active ('x'/'n' are code-alphabet letters, and the pause zone was the original stray-P source — taps still reach `touch_tap` on release for keyboard re-summon). iOS keeps the TEXTINPUT-driven path (system keyboard always commits through insertText). Also, lobby touch UX per Glenn's screenshot: header + code entry hoisted toward the top edge with wider spacing (keyboard eats the bottom half), and the footer is now a "RETURN TO MENU" tap band (bottom strip, every lobby screen) instead of the "ESC - BACK TO MENU" key hint. Desktop room-flow e2e re-verified.
- **2026-07-05 (u)**: **Web hidden-tab starvation (Glenn: unfocused tab "only gets certain updates, infrequently").** Browsers stop requestAnimationFrame in hidden/occluded tabs, so the Emscripten main loop barely runs: a hidden host stops simulating (the peer's game stalls to bursts), a hidden client applies queued snapshots in rare clumps. Fix: a plain `setInterval` heartbeat (500 ms nominal; browsers clamp to ~1 s hidden — and pages with a live WebRTC connection are exempt from Chrome's harsher 1/min intensive throttling) calls the exported `web_background_tick()`, which pumps the main loop only while `document.hidden` and `StateManager::wants_background_ticks()` (net game or NetLobby) — solo play stays frozen-as-pause while hidden, as before. A 2 s catch-up cap stops a harder-throttled tab from simulating a minute of world time in one call. The dead-man switch tolerates the 1 Hz cadence (it only releases held inputs after 1 s, which is correct for a player who isn't looking). Web-only code — compiles on master's web.yml; needs a two-tab test with one hidden.
- **2026-07-05 (t)**: **iOS CI selftest green + Android device playtest batch (Glenn, first on-device run).** iOS run #1842: the simulator boots, installs the .app and the loopback selftest prints NET SELFTEST PASS in CI — first runtime verification of netplay on iOS (the previous run only failed on macOS runners lacking GNU `timeout`; the launch is now wrapped in `perl -e 'alarm shift; exec @ARGV'`). Android findings, all fixed:
  - **Touch main menu rebuilt**: "tap to start" is now a real attract screen (as on desktop) that dismisses into the desktop-style vertical row menu — CONTINUE / NEW GAME / ONLINE as equal-size tappable rows sharing the desktop layout geometry (`menu_row_at()` mirrors `draw_menu_rows()`), replacing the side-by-side halves with ONLINE squeezed underneath at a smaller size.
  - **Stray "P" in the join code on every keyboard summon**: the tap that opens code entry lands in the centre-screen pause zone, and `finger_up` synthesizes `keyboard_up('p')` — which `NetLobby::keyboard_up` fed straight into the code field ('P' is in the code alphabet). Code entry moved to key-DOWN (`keyboard()`, where real chars arrive: GLUT press events, SDL_TEXTINPUT on mobile); key-up is suppressed on touch in menu+lobby (touch_tap owns interaction); android/ios event loops drop printable KEYDOWN/KEYUP while text input is active (SDL synthesizes key events for committed IME chars — forwarding both would double-type).
  - **Client touch OSD missing + can't move (reported on rejoin; actually any mobile client join)**: the OSD gate and `touch_joystick` both hardcoded `players->front()` — on a net client the local ship is `players->back()`, so the joystick drove the remote ghost and the OSD never drew (shoot worked because the key bindings ARE moved to the local ship). New `GLGame::local_player()` used by both.
  - **P1 exhaust trails invisible on P2's screen**: trail emitters key off `thrusting`/`reversing`/`rotation_direction`, which snapshot restores cleared on the remote ship. Ship extras now carry a move_flags byte (**PROTO 4→5**; both peers must rebuild) and the client applies it to non-local ships (raw flag writes — `Ship::thrust()` touches the shared boost-sound volume).
  - **Lobby touch polish**: share tap band now centres on the drawn "TAP HERE TO SHARE IT" line (was ny>=0.72, i.e. *below* the text, over "WAITING FOR PLAYER 2..."); code entry draws in the TOP half on touch so the soft keyboard doesn't cover the typed code (it updates per keystroke); taps during code entry no longer flash the length hint.
  - Verified: desktop room-flow e2e green on PROTO 5 (offer 0 s, in-game 4 s, no decode errors), loopback selftest PASS, non-netplay desktop build clean. Touch flows need Glenn's next APK run.
- **2026-07-05 (s)**: **M2 decisions audit — the last unimplemented commitment closed.** The M2 decision "default endpoint baked in, overridable via `signal_url` in the preferences INI" had only the env-var half; `signal_url=` in preferences.ini now works (precedence: web URL param > NEWTONIA_SIGNAL_URL env > INI > baked default), persisted when set, and both entry points load preferences before the signal self-test so the override applies there too. Verified: selftest resolves the INI URL against a local relay, env still wins over INI, and — bonus discovery — the container can now reach the PRODUCTION relay (selftest PASS against Glenn's deployed worker, confirming the M3-1 worker deploy is live). Milestone 2 is now fully delivered; the one explicitly-deferred optimization from its decisions is trickle ICE ("a later optimization now that a live channel exists").
- **2026-07-05 (r)**: **Remote respawn ghost fixed (Glenn: P1 briefly at a wrong location for P2 after death).** Root cause found by instrumenting drawn-vs-authoritative error per apply: `Ship::step` decrements `time_until_respawn` and SELF-RESPAWNS when it crosses zero — on the client this races the host's authoritative respawn by tick skew, resurrecting the copy at a RANDOM `safe_position` (and charging a phantom life); while the snapshot still said `respawning`, the state loop skipped the position pin, so the ghost stood at the random spot until the host's real spawn arrived. Fix: in `tick_net_client` a dead ship's countdown is held just above zero (the HOST owns respawns; the snapshot alive-transition resurrects at the authoritative spawn). Also the extras fallback resurrect now re-fires the spawn flash at the pinned position (it detonated at the interim random spot). Verified by e2e ramming the host ship into asteroids: before — deaths produced 600-2500-unit alive-to-alive ghost jumps; after — every death resurrects through the restore path, the only large one-apply discontinuities left are level-skip spawns (pinned same-apply, never drawn wrong).
- **2026-07-05 (q)**: **M3-1 LANDED — mobile lifecycle & host room reclaim.** Worker: rooms survive host-socket loss for HOST_GRACE_MS (2 min); the room frame carries a reclaim token (`{t:"room", code, token}`); `role=host&code=X&token=Y` reclaims the slot (fresh hosts can't squat in-grace codes; joiners may join during grace and wait); the stored offer is cleared when a JOINER drops (replaying it to a rejoiner dead-ended the handshake — the host re-offers and that copy is stored instead; the lobby also re-offers on PeerLeave). **The worker must be redeployed** — the game degrades gracefully against a token-less relay (old freeze behaviour). Game host: `net_host_signal_maintain` runs every healthy tick — detects signal Closed, reclaims with 3 s retries (also mid-rejoin), and treats PeerJoin-while-healthy as instant client-loss detection (the client only rejoins the room if its transport is dead; ICE alone takes ~10+ s to notice). Client: keeps the room code; on connection loss it auto-rejoins after 1.5 s via `NetLobby(code)` (the entire existing join/bootstrap path — zero new sync code), `focus_gained` fast-paths the countdown for mobile resume; the CONNECTION LOST card becomes "REJOINING..."; any key still exits to the menu. Verified: 14-check node protocol test (`signal/test/reclaim_test.js` against wrangler dev) + full-loop Xvfb e2e (env hooks `NEWTONIA_NET_TEST_DROP_SIGNAL_MS` / `_TRANSPORT_MS` sever the sockets on a timer): relay drop -> "room reclaimed"; transport drop -> client "auto-rejoining" -> host "player 2 rejoined" -> snapshots resume. No PROTO bump (no wire-format change on the data channels). **Follow-up (same day)**: the auto-rejoin lobby RETRIES the relay (5 s cadence, 90 s budget ~ the grace window, fresh virgin transport + ICE per attempt) instead of falling back to the manual screen — a mobile client's own network is usually still down when the 1.5 s rejoin fires. Fatal room errors (no-such-room/expired/room-full) fail fast to "THE ROOM IS GONE"; rate-limited waits 15 s. Verified with a TCP-proxy e2e (scratchpad wsproxy.py): joiner's relay path severed, transport dropped, two offline retry cycles logged, reconnect on the first attempt after the "network" returned.
- **2026-07-05 (p)**: **Client-authoritative aim (PROTO 3->4)** — Glenn: P2's bullets left the nose at a visibly wrong angle, left or right, "as if quantised". Root cause: the client's local aim is deliberately never snapshot-corrected (aiming must feel instant), but the host reconstructed P2's facing from the on/off rotate held-bits at its own rate — rotation inertia (the ease-out after key release), analog stick rates, sensitivity prefs and input latency all diverge, and with no correction the error accumulates for the whole session. MSG_INPUT now carries the client's exact facing vector; the host adopts it (normalized, magnitude sanity 0.5-2.0) on every INPUT and the held bits only extrapolate between arrivals. Loopback self-test + lobby e2e green. Both peers must be on v4 — older builds fail the version check cleanly at handshake.
- **2026-07-05 (o)**: **Mac universal-bundle playtest batch (Glenn):** net overlays drew in window pixels instead of Typer's virtual HUD units → offscreen in fullscreen (fixed + Xvfb-verified at 1600x1000); "random" short shield hums were the remote ship's spawn-invincibility hum (player ships are never distance-muted) → online the hum is now local-only on both sides; 5/S dropped from the room-code alphabet (identical in the game font — worker must be redeployed together with the client); SIGSEGV when the host finished a level (or paused) while P2 was disconnected — net_send_event dereferenced the null torn-down session → no-op without a session (repro'd + fixed under Xvfb: SIGKILL joiner, skip level, host reaches LEVEL 2); disconnected player's ship now parks IN PLACE with the shield up (invincible, motionless, inputs cleared) instead of vanishing — per-tick re-assert survives level rebuilds (which reset the invincibility window), rejoin drops the shield to the normal 1.5 s grace and resumes in place, a dead-at-loss ship still parks as a frozen corpse (no life bleed).
- **2026-07-05 (n)**: **MbedTLS builds silently regressed to the clipboard flow — signal wss:// TLS fix.** Glenn's universal mac bundle showed "the old invite screen instantly": the signal socket died in ~60 ms (SIGNAL SELFTEST FAIL) and the lobby's error fallback is the M1 manual flow, indistinguishable from old code. Root cause in libdatachannel v0.24.5: wss:// with a hostname uses `VerifiedTlsTransport` = `MBEDTLS_SSL_VERIFY_REQUIRED`, and MbedTLS has NO OS trust-store integration — with no CA file configured every certificate is rejected. OpenSSL builds passed only because OpenSSL finds the system/Homebrew CA store by itself; upstream hard-disables verification on `_WIN32` (which is why Windows room codes worked on the MbedTLS Windows build). The C API's `rtcWsConfiguration` exposes no CA field, so `net_signal_rtc.cpp` now creates the socket via `rtcCreateWebSocketEx` with `disableTlsVerification = true` — parity with upstream's own Windows behaviour on every platform; game-channel security is independent (DTLS authenticates via SDP fingerprints). Reproduced and verified in-container: local MbedTLS libdatachannel + `wrangler dev --local-protocol https` (self-signed cert) → selftest FAIL before the fix, PASS (full room-code round trip) after. Future hardening if wanted: switch the signal backend to the C++ API (`WebSocketConfiguration::caCertificatePemFile`) with a bundled CA PEM. Why MbedTLS at all: Homebrew OpenSSL is thin per-prefix (arm64 in /opt/homebrew, x86_64 in /usr/local) and its CA paths don't exist on end-user machines, so the universal bundle needs a from-source fat TLS lib; MbedTLS cross-compiles fat in one cmake invocation.
- **2026-07-05 (m)**: **CI red on two fronts (Glenn), both fixed in 3a365b0; universal `make osx NETPLAY=1` now CI-proven.**
  - **Windows (six runs red)**: MinGW's Windows headers define `near` as a legacy empty macro, so the join-spawn re-roll's `Point near` in `add_remote_player` was a syntax error on Windows only (mesh.cpp/gles2_compat.cpp already `#undef near/far` for the same reason). Renamed to `nearest`. Lesson: Linux/mac builds are no shield against the `near`/`far` macro trap.
  - **macOS (universal netplay deps)**: `MBEDTLS_USER_CONFIG_FILE="path"` lost its quotes between CMAKE_C_FLAGS and the generated compile line (shell eats them), so mbedtls hit `#include <bare path>` → `expected "FILENAME"`. Fix: backslash-escape the quotes in `build_netplay_deps.sh` (`\\\"$ROOT/...\\\"`) so the compiler receives a real `"filename"` token; mechanism verified locally with a throwaway CMake project before pushing.
  - macOS Dev #1330 green with the full gauntlet: fat MbedTLS+libdatachannel, `make osx NETPLAY=1`, lipo asserts arm64+x86_64 on binary and embedded dylib, loopback self-test from inside the bundle. Windows #1993 green.
  - Note: CI's netplay builds are verification gates only — the shipped mac .app artifact (and the Windows .exe) is still the non-netplay build; flipping the artifacts to netplay is a small workflow change, on request.
- **2026-07-04 (l)**: **Windows playtest fixes (Glenn) — one root cause behind three symptoms, plus lobby/transport UX:**
  - **Client restore churn was the big one**: `Ship::restore_state` runs `respawn()`, which (a) resurrected a host-side-dead ship every snapshot (burning a life each time), (b) relocated the ship to a RANDOM spot via `safe_position()` whenever the authoritative position was within 50 units of an asteroid, and (c) force-set `invincible=true`. Symptoms as seen: "ship teleports to random locations with no input" (host-side deaths were invisible — the corpse got resurrected before the player saw it die, then the host's respawn warped it), "shield rapidly toggles on both players" (10 Hz invincible flicker), "player can still input controls during their countdown" (the resurrected corpse still responded). Fixes: skip the full restore while the snapshot says `respawning` (HUD scalars only; the extras' alive-transition respawns for real), pin the authoritative position after every restore, and set `invincible` from the extras' authoritative fields.
  - **Input parity at discontinuities**: `net_held_suppress_` — at level transitions and while the remote ship is dead, its held INPUT bits are ignored until the client reports each key released once (respawn's `reset()` gives the local player exactly that restriction); the client mirrors it by not re-applying held state across a world-rebuild restore.
  - **Native 8 s gathering fallback** (mirror of the web backend's): slow/filtered STUN (Windows firewall) stalled invite-code generation for tens of seconds; after 8 s the SDP is snapshotted with whatever candidates exist (host candidates suffice for LAN/same-machine). Self-test PASS via the fallback path.
  - **Joiner connect timeout removed** (the fallback exposed it, but it bit humans all along — the "COPY THE REPLY AGAIN" Glenn saw on mac): the joiner's WaitConnect includes the humans ferrying the reply code (minutes); only the HOST keeps the 25 s clock, since after pasting the reply the connection either establishes in seconds or never. Also: adopt_channel now checks `rtcIsOpen` after attaching callbacks (open events don't retro-fire; the join side could lose that race).
  - e2e re-verified after all of the above: lobby connects (fallback SDPs at 8 s, 1 host candidate each), level skip produces warp snaps, no crashes. Note for e2e drivers: idle ships in a fresh asteroid field legitimately die and respawn at random spots repeatedly — multiple warp snaps in a log are not by themselves a bug.
- **2026-07-04 (k)**: **Explicit warp flag (per Glenn — a new-level spawn still interpolated on his build)**. The 250-unit distance heuristic is now a backstop; the primary signal is explicit: `Ship::net_warp_count` (u8, bumped in `Ship::respawn` and `Teleport::step` — every discontinuous move site) travels per-ship in the snapshot NetExtras; when the client sees its local ship's count change it snaps position/velocity/facing to the snapshot unconditionally ("net: warp snap" log). First snapshot baselines the count. **PROTO_VERSION 1→2** (extras record grew a byte — mismatched builds now fail the handshake cleanly instead of misparsing extras); both peers must be on this build or newer. Verified via `drive_snap.sh` e2e (host skips level → joiner logs warp snap count 1).
- **2026-07-04 (j)**: **Discontinuity snap (5b77d22)** — Glenn: new-level spawns (and teleports/respawns) interpolated the client ship from its old position. The 0.35 prediction blend was unconditional; corrections beyond 250 units (far past any prediction error) now hard-snap position, velocity and facing to the authoritative pose, with a `net: local ship snapped N units` log line. Verified via Xvfb e2e (scratchpad `drive_snap.sh`): joiner flies from spawn, host skips level → joiner logs a 985-unit snap and is centered on the new level within one snapshot. Note the log line only flushes to a redirected file at exit (stdout block buffering).
- **2026-07-04 (i)**: **Native↔web cross-play verified — Milestone 1 verification checklist complete** (modulo the Windows two-exe run, which is Glenn's whenever a Windows box is handy; mac↔mac native and linux↔linux native pairs, web↔web tabs, and native↔web cross are all green). Cross-test note: the differing builds interoperate cleanly — savegame-body + NetExtras byte layouts agree between clang/wasm and g++/x86-64 (all little-endian, bool=1). Remaining known M1 rough edges (accepted scope): local-ship rubber-banding under hard acceleration, own-bullet flicker at snapshot boundaries, remote one-shot sounds missed between snapshots, no rejoin (M2 signaling makes it cheap — any snapshot can bootstrap a client).
- **2026-07-04 (h)**: **Phase 8 landed (1a644cf) plus four playtest-driven fixes** (Glenn playtested native mac↔mac):
  - `build_netplay_deps.sh` replaces the bogus `brew install libdatachannel` recipe (no such formula; macos-dev.yml now runs the script + loopback self-test on a real Mac runner).
  - Held-input wipe: snapshot `restore_state` respawn/reset cleared held flags 10×/s; full held state now captured/re-applied (movement felt like "a few ticks then reset").
  - Client shooting dead: semi-autos self-clear their trigger, so sampling the weapon state sent held=0; now GLShip mirrors key INTENT, and INPUT carries wrapping shoot/secondary PRESS counters with an edge-arm rule on the host (press arms, release disarms, held-no-press leaves alone) — semi-auto = one shot per press, automatics spray, shield holds, no weapon introspection.
  - **ICE paste race** (the big one for real users): the joiner's ICE gave up ~10 s after pasting the invite, but humans take minutes to ferry the reply. Joiner now strips a=candidate lines from the offer (`Net::strip_ice_candidates`) so its agent idles with nothing to time out on; the host initiates all checks when the reply is pasted and the joiner answers via peer-reflexive discovery. Also: DISCONNECTED no longer latches failure (transient per spec) on either backend. Reproduced + fixed via the Xvfb e2e with a ~35 s paste gap.
  - Phase 8 proper: pause sync, CONNECTION LOST card, generation banner, dead-man switch, best-effort BYE (details in the phase line above).
- **2026-07-04 (g)**: **Phase 7 landed (fac5a77) — the milestone. Two machines play online co-op, native and web.**
  - Client bootstrap: joiner lobby assembles snapshot #1 → `GLGame(snapshot, session, ctrl)` (delegates to the save-restore ctor) → strips the remote ghost's bindings, gives the local (last) ship player-1 keys/controller.
  - Input sampling needed no per-key plumbing: held bits read from Ship state + `(*primary)->is_shooting()`; one-shots are wrapping counters bumped inside `Ship::boost/next_weapon/next_secondary_weapon` and GLShip's teleport/respawn-tap sites, so keyboard/controller/touch all count automatically.
  - Local-ship correction: 0.35 lerp toward the authoritative pose (wrap-aware via `closest_to`), aim stays fully local, and held fire is re-applied after `restore_state` rebuilds the weapon list (otherwise every snapshot would silently release the trigger — subtle!).
  - **Mac native testing** (Glenn): libdatachannel is NOT in Homebrew — build it from source: `./build_netplay_deps.sh` (needs `brew install cmake openssl@3`), then `make clean && make NETPLAY=1 NETPLAY_PREFIX=$PWD/netplay-libs`. Sanity check: `NEWTONIA_NET_SELFTEST=1 ./newtonia` prints NET SELFTEST PASS. Then run two copies, ONLINE → HOST/JOIN, codes via cmd-C/cmd-V between them. Cross-plays with web + Windows. The GLUT build inits SDL video under NETPLAY for the clipboard API; macos-dev.yml CI runs this exact recipe.
  - **Steam invite (`connect`) testing** (`invites.h/cpp` + `steam_invites.cpp`): the "Join Game" button is friend→friend (needs a second Steam account), but the join *plumbing* — a cold-launch Join hands the joiner `+connect <code>` on the command line, read by `Invites::capture_launch` — is testable solo. Host with the plain build (`./newtonia` → ONLINE → HOST → read code), then join with the connect arg:

    ```sh
    ./newtonia-steam +connect <CODE>              # direct: exercises capture_launch → NetLobby(<CODE>)
    open "steam://run/4536720//+connect <CODE>/"   # via Steam: proves Steam forwards the connect string to the depot build
    ```

    The `steam://` form launches the *installed depot* build, so it only joins once a tag carrying the invite code (≥ netplay-v1.41.31) has deployed AND Steam has updated — an older installed build opens but ignores the arg. The running-game accept path (`GameRichPresenceJoinRequested_t`, friend clicks Join while the joiner is already open) still needs a real friend. The advertising half (the `connect` key that makes the button appear) is confirmed via the Steamworks Rich Presence Tester.
  - Verified: (1) native↔native — scratchpad `drive_netplay.sh` drives two instances under one Xvfb through the real lobby, clipboard signaling over the shared X11 selection; gathering takes ~25 s/side here (UDP blocked → STUN timeout) but connects instantly on loopback; both HUDs live, joiner thrust moved its ship across the host's minimap. (2) web↔web — `pwtest/coop_test.js`, both tabs through the real UI, transports healthy after play. Xvfb gotcha: two GL windows + no compositor → `xwd` of the occluded window is black; `xdotool windowraise` before each capture.
  - M1 cuts inside Phase 7: client god-mode/shield visual flourishes; enemies list not extras-synced (station's enemies restore via `GLStation::restore_state` at 10 Hz).
- **2026-07-04 (f)**: **Phase 6 landed (b5c337b).**
  - Host ctor delegates to the fresh-game ctor then flips `net_mode_`, adopts the session, adds the bindings-less GLCar. Lobby hands off on handshake Ready (host only; joiner keeps draining snapshots on its waiting screen until Phase 7).
  - INPUT apply: first message baselines the one-shot counters (no stale bursts), counter deltas capped at 4, stale seqs dropped via signed wrap-aware compare. Respawn tap mirrors GLShip::input's grace-period rule.
  - NetExtras layout (raw native-endian like the savegame body it follows): u32 player count, per ship {alive u8, temperature f32, time_until_respawn i32, time_left_invincible i32, god_ms i32, shield u8, then bullets/mines/giga_mines as u16 count + pos/vel f32x4, missiles +facing+time_left, shockwaves pos/radius/max/speed/time_left/is_nova}, then u32 asteroid count + net_ids (same order as the save body's asteroid array).
  - **Two pre-existing-shaped bugs found by the e2e** (both `players->size()` used as a split-screen proxy; online = 2 players, 1 viewport): `draw_map` drew the split divider + centre minimap, and `setup_viewport`'s portrait hack (`window.x() <= window.y()`) flipped the primary view into the second-player slot — with ny=1 that's `glViewport(0, window.y(), ...)`, fully off-screen, i.e. a black world view. Both now use `split_screen()`. (Surfaced because the web canvas is square; a landscape window would have hidden the viewport one.)
  - Verify tooling note: the host e2e (scratchpad `pwtest/host_test.js`) parses player 2's position straight out of reassembled snapshot payloads in JS — useful again for Phase 7.
- **2026-07-04 (e)**: **Phase 5 landed (beee201).**
  - `NetSession` phases: Connecting → Handshaking → Ready/Rejected/Failed; client HELLOs on connect (u16 `Save::GameState::VERSION` + reserved u32 build id — a real build hash would break native↔web cross-play, so M1 checks save-format version only); host WELCOMEs (player_id=2, step=8 ms, snapshot=100 ms) or REJECTs; 10 s handshake timeout. Session owns the transport from signaling-complete onward — GLGame will take the session in Phase 6.
  - `NetLobby` screens: Choose → HostGathering→HostWaitAnswer / JoinWaitOffer→JoinGathering → WaitConnect → Connected | LobbyFailed(retry). V = paste (clipboard read starts inside the gesture), C = re-copy, controller A/X/Y/B mapped. 25 s connect timeout. UX language: "INVITE CODE"/"REPLY CODE" instead of offer/answer.
  - Menu ONLINE row: `show_online_row() = net_available() && !is_touch_mode()`; row lists are data-driven now. Options row stays last, so the existing `menu_selection == max-1` beta check is untouched.
  - **Verified**: (1) two-tab Playwright e2e on the real web build — attract → menu → lobby → clipboard hand-off → CONNECTED both sides, screenshots reviewed; (2) native Xvfb smoke — ONLINE hidden without a backend, menu/NEW GAME regress-free; (3) codec unit tests. **Gotcha**: `Typer` has no glyphs for parentheses — avoid them in lobby text.
- **2026-07-04 (d)**: **Phase 3 landed (6c66a0f).**
  - Web backend is handle-based (`Module.__nwnet.conns[h]`) so multiple transports coexist like native, and the whole glue is drivable from the browser console. `nwtest_*` KEEPALIVE exports = the web self-test: one transport per page, driven from JS (a blocking in-process loopback is impossible on the single-threaded web main thread). `ccall` added to `EXPORTED_RUNTIME_METHODS` for their string args.
  - **Gathering gotcha (matters for real users too)**: Chrome can sit in `iceGatheringState: 'gathering'` indefinitely (slow/unreachable STUN, odd interfaces) and never fire 'complete'. Fix: 8 s post-`setLocalDescription` snapshot fallback (`descTimeoutMs`) — the SDP already embeds all candidates gathered so far. Without this the lobby would hang on such networks.
  - **Verified**: local `make web` with emsdk latest (6.0.2, same as web.yml) + headless-Chromium Playwright test: two pages of the real game build → offer/answer via the C++ path → CONNECTED → rel+unrel bytes both directions → clipboard round-trip. Chromium needs `--disable-features=WebRtcHideLocalIpsWithMdns` in containers (no multicast for mDNS candidates); real browsers on real LANs are unaffected (mDNS resolves there, and STUN provides srflx).
  - **CI warning**: web.yml's `deploy` job is NOT branch-gated — a `workflow_dispatch` on a feature branch would deploy that branch to production GitHub Pages. Do not dispatch it for testing; verify web builds locally (emsdk install steps mirror web.yml). Dev-container note: emscripten's ports fetcher can't download GitHub archive zips through the egress proxy (403) — pre-seed `~/emsdk/upstream/emscripten/cache/ports/{sdl2,sdl2_mixer}` from git clones at the pinned tags + matching `.emscripten_url` marker files.
- **2026-07-04 (c)**: **Phase 2 landed (da835b3), all 7 workflows green.**
  - `xbox/CMakeLists.txt`: `NEWTONIA_NET` option (ON; forced OFF when `CMAKE_GENERATOR_PLATFORM MATCHES "Gaming\.Xbox"`), FetchContent block with the three spike fixes, `NEWTONIA_NET_RTC` define + static link on the game target. Adds ~1 min to the xbox-dev job (4:30 vs 3:30).
  - `net_transport_rtc.cpp`: real backend — libdatachannel C API, callbacks enqueue-only (mutex deque + atomics), non-trickle SDP at gathering-complete, channels identified by id (host-created) or label (join side via `rtcSetDataChannelCallback`). Channel close while open ⇒ `failed()`.
  - `net_selftest()` (net_transport.cpp, gated on `NEWTONIA_NET_RTC`): host+join loopback **through the public NetTransport API** with bidirectional echo on both channels — stronger than the Phase 0 spike, which bypassed the game's code. Hooked in xbox_main.cpp on `NEWTONIA_NET_SELFTEST=1` **before SDL_Init** (headless-safe; ANGLE DLLs still needed — import table), logs to `newtonia.log`, exits 0/1. xbox-dev.yml runs it after Stage DLLs, so every push now exercises a real WebRTC loopback on Windows. Also verified natively on Linux with the same pins (gathering takes the full ~23 s STUN timeout in the dev container where UDP is blocked — falls back to host candidates and still connects; instant on CI/normal networks).
  - **Testable now**: download the `newtonia-gdk-desktop` artifact, set `NEWTONIA_NET_SELFTEST=1`, run `newtonia.exe`, read `newtonia.log`.
  - Next: Phase 3 (web backend). The Phase 0 spike + `netplay-spike.yml` are now redundant (xbox-dev covers the same ground with the real backend) — delete in a later cleanup once netplay stabilises.
- **2026-07-04 (b)**: Phases 0-commit/1/4 this session (remote Linux container; Windows spike runs via CI instead of Glenn's box).
  - **Phase 0**: spike committed as `spike/netplay/` (`main.c` is deliberately C, not C++ — the Android CMake `GLOB_RECURSE *.cpp` would otherwise suck it into the game build) + `netplay-spike.yml` (windows-latest, vcvars64+Ninja like xbox-dev.yml; builds, checks `dumpbin /dependents` for dynamic-CRT imports, runs the loopback, requires exit 0). Two FetchContent interop findings so far, both now fixed in the spike CMake and **both needed again in Phase 2's xbox/CMakeLists.txt block**:
    1. libdatachannel's `FindMbedTLS.cmake` only searches installed distributions — but its CMakeLists skips `find_package` when `MbedTLS::MbedTLS` already exists, so alias `mbedtls`/`mbedx509`/`mbedcrypto` → `MbedTLS::MbedTLS`/`::MbedX509`/`::MbedCrypto` after the MbedTLS FetchContent.
    2. Its `install(EXPORT LibDataChannelTargets)` fails at generate because the in-tree mbedtls target is in no export set → `set(CMAKE_SKIP_INSTALL_RULES ON)` (nothing is installed anyway).
    3. Its MbedTLS DTLS transport (`src/impl/dtlstransport.cpp`) references the DTLS-SRTP API unconditionally — even with NO_MEDIA — and MbedTLS's default config compiles that API out → `add_compile_definitions(MBEDTLS_USER_CONFIG_FILE="…/mbedtls_user_config.h")` (a one-line header defining `MBEDTLS_SSL_DTLS_SRTP`) applied to the whole build tree so library and consumers agree on config.
    **Run 4 = SPIKE PASS** (84e3145): offer/answer, both PCs connected, "rel"+"unrel" channels open, binary echoes on both, exit 0; dumpbin shows only WS2_32/bcrypt/KERNEL32 imports (static CRT confirmed). Whole job ~3.5 min — cheap enough to keep as a canary until Phase 2 replaces it.
    Good news from run 1: the v0.24.5 pin's submodules (libjuice/usrsctp/plog) fetch fine via FetchContent.
  - **Phase 1**: seam landed (955e357), no behavior change, all platform workflows green. `net_protocol.h` carries the header/enums + LE byte-packing helpers and a bounds-checked `Net::Reader`; message-specific encode waits for Phase 5. Note web.yml only triggers on master/main, so branch CI doesn't cover the Emscripten glob — web stub was syntax-checked locally with `-D__EMSCRIPTEN__`.
  - **Phase 4**: Stream refactor landed (510cf0b) with byte-identical proof: synthetic GameState exercising every format branch (all asteroid specials incl. tough cracks, station+enemies, mini-station, all pickup types, black hole, 2 players, god-mode weapon entry) → sha256-identical `savegame.dat` before/after refactor, plus MemStream serialize→deserialize→serialize == file body. Harness lives in the session scratchpad only; re-create from this description if needed.
- **2026-07-04**: Plan approved. Pins verified via `git ls-remote` (MbedTLS v3.6.6, libdatachannel v0.24.5). Phase 0 spike written: a scratch CMake project + C-API loopback `main.c` exercising `rtcCreateDataChannelEx` with `reliability.unordered/unreliable/maxRetransmits=0` — the exact struct the real backend needs. CMakeLists = FetchContent MbedTLS v3.6.6 (`ENABLE_PROGRAMS/TESTING OFF`) + libdatachannel v0.24.5 (`USE_MBEDTLS/NO_MEDIA/NO_WEBSOCKET/NO_EXAMPLES/NO_TESTS ON`), `CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>"`, link `LibDataChannel::LibDataChannelStatic`; main.c = in-process two-PC loopback (desc/candidate callbacks cross-wired, rel + unrel channels, binary echo round-trip, prints SPIKE PASS/FAIL). **Spike not yet run**: on Glenn's Windows box `cmake` is not on PATH; VS 2022 **Build Tools** live at `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools` (found via vswhere) — run the spike from a `vcvars64.bat` environment (bundled cmake/ninja), same as `.github/workflows/xbox.yml` does.

---

# Appendix: full approved plan (Milestone 1)

## Context

Newtonia currently has local-only 2-player (split-screen, one machine) and zero networking code. Goal: **playable online 2-player co-op** with full cross-platform reach as the end-state. Decisions made with the user:

- **Transport**: WebRTC DataChannels — the only UDP-like transport a browser supports. Native (Windows) uses **libdatachannel** via its **C API** (`rtc/rtc.h`) so the game stays C++11 (libdatachannel compiles C++17 in its own static lib); web uses the browser's `RTCPeerConnection` via Emscripten interop.
- **Topology**: P2P, **host-authoritative** — host simulates everything; client sends input and renders snapshot-corrected extrapolation.
- **No PlayFab, no Steam networking, no signaling server yet**: Milestone 1 signaling is manual clipboard copy-paste of base64 SDP offer/answer. Room codes + hosted signaling are a later milestone (signaling sits behind a small interface for the swap).
- **Players**: 2 now; protocol carries `player_id` + version byte so N-player is possible later.
- **Milestone-1 platforms**: Windows native (`xbox/CMakeLists.txt` GDK Desktop — the active CI target) + Web (`make web`). Linux/macOS Makefile netplay is a documented stretch; Xbox console / Android / iOS get a compiled-but-disabled seam so all CI stays green.

## New files (picked up automatically by every build's source glob)

| File | Purpose | Gate |
|---|---|---|
| `net_transport.h/.cpp` | abstract transport + `NetTransport::create()` factory (returns `nullptr` where unavailable); `net_available()` inline | none — compiles everywhere |
| `net_transport_rtc.cpp` | native libdatachannel C-API backend | whole file `#ifdef NEWTONIA_NET_RTC` |
| `net_transport_web.cpp` | Emscripten backend: EM_JS/EM_ASM inline JS (`Module.__nwnet` with pc/channels/inbox, poll-style like the existing `web_on_idb_ready` pattern) + async clipboard helpers | whole file `#ifdef __EMSCRIPTEN__` |
| `net_protocol.h` | message enums, packed encode/decode (explicit byte packing, never memcpy structs) | none |
| `net_session.h/.cpp` | handshake, snapshot chunking/reassembly, input encode, base64 | none |
| `net_lobby.h/.cpp` | new `State`: HOST/JOIN flow, clipboard signaling UX, status text via `Typer::draw_centered`, starfield background | none (menu hides ONLINE when `net_available()` is false) |

`NEWTONIA_NET_RTC` is defined only by `xbox/CMakeLists.txt` (new `NEWTONIA_NET` option, ON for GDK Desktop, forced OFF for `XBOX_SCARLETT`/console). xbox-console-smoke.yml compiles the new root files as near-empty TUs (no define, no link step) — green by construction.

### Transport interface (net_transport.h)

`start_host()`, `start_join(offer)`, `local_description_ready()` (gathering complete — non-trickle, all ICE candidates embedded in the pasted SDP), `local_description()`, `set_remote_answer()`, `connected()`, `failed()`, `send_reliable()`, `send_unreliable()`, `poll(out)` (main-thread pop), `close()`.

Native impl: one peer connection (STUN `stun:stun.l.google.com:19302`), two channels — `"rel"` (ordered/reliable: handshake, snapshots, events) and `"unrel"` (`unordered, maxRetransmits=0`: input). libdatachannel callbacks fire on worker threads → mutex-guarded inbound deque + atomic state flags; callbacks only enqueue. `<mutex>`/`<atomic>` are C++11.

## Build integration (xbox/CMakeLists.txt)

FetchContent **MbedTLS v3.6.6** (avoids OpenSSL-on-CI pain) then **libdatachannel v0.24.5** (`USE_MBEDTLS=ON, NO_MEDIA, NO_WEBSOCKET, NO_EXAMPLES, NO_TESTS`), link `LibDataChannel::LibDataChannelStatic`. /MT is inherited via the existing global `CMAKE_MSVC_RUNTIME_LIBRARY` (CMP0091 NEW, lines 7/118). libdatachannel vendors libjuice/usrsctp/plog as git submodules — test that the pin fetches them (avoid GIT_SHALLOW if it breaks submodules). **No workflow file edits needed** (xbox.yml's configure-retry loop already covers fetch flakes). Web needs no Makefile changes (EM_JS lives in a normal .cpp).

## Protocol (little-endian; 4-byte header: proto_version, msg_type, player_id, reserved)

- `HELLO` (C→H, rel): proto version + `Save::GameState::VERSION` + build hash → `WELCOME` (player_id, step_size, snapshot period) or `REJECT`.
- `INPUT` (C→H, **unrel**, every 8 ms tick): uint32 seq; uint16 held-button bitmask (left/right/thrust/reverse/shoot/secondary); wrapping uint8 counters for one-shots (boost, next_weapon, next_secondary, teleport, respawn-tap) so lost packets can't drop or double-fire them; 3 analog floats. Host ignores stale seq; **dead-man's switch** zeroes held state if input stops >1 s.
- `SNAPSHOT_CHUNK` (H→C, rel, 10 Hz): snap_id + chunk idx/count + bytes; chunks of **15 KB** (safe under browser DataChannel interop limits). Payload = savegame serialization + `NetExtras` (per-ship transients the save format omits: alive/temperature/respawn/invincibility/god-mode/shield + the public projectile vectors `bullets/missiles/mines/giga_mines/shockwaves` (ship.h:99–101) + asteroid `net_id` array). ~20 KB × 10 Hz ≈ 1.6 Mbps — fine for M1; delta snapshots deferred.
- `EVENT` (rel, both ways): PAUSE / RESUME / GENERATION_START(+gen) / GAME_OVER / BYE.

## Savegame reuse (savegame.h/cpp — contained mechanical refactor)

Add `Save::Stream` (virtual write/read), `FileStream` (fwrite/fread) and `MemStream` (vector-backed); change `wv/rv/wa/ra` (savegame.cpp:24–34) and every per-type helper from `FILE*` to `Stream&`; expose `serialize_game`/`deserialize_game` (no magic/version — `save_game`/`load_game` keep wrapping them). **No disk-format change**; verify byte-identical saves before/after (`fc /b`). `net_id` travels in NetExtras, NOT in the save record — the append-only savegame convention is untouched, no VERSION bump.

## Sync model (the core design decision)

**Snapshot-driven client with local extrapolation** (not deterministic lockstep — `rand()` is seeded per-machine and used everywhere; only the host rolls dice).

- **Host**: normal sim. Start of `tick()`: poll net, `apply_remote_input()` to player 2; end: every 100 ms, `build_save_data()` (exists, glgame.cpp:352) + NetExtras → serialize → chunk → send.
- **Client bootstrap**: NetLobby waits for snapshot #1, then uses the **existing** `GLGame(const Save::GameState&, …)` constructor (glgame.cpp:198) — full world reconstruction for free.
- **Client per-frame**: new `tick_net_client()` runs the same 8 ms accumulator but only visual/kinematic stepping (asteroid/pickup/projectile motion, particles, trails) — **no** collide/drops/kills/generation logic. Local ship predicts from local input; every tick an INPUT message is sent.
- **`apply_snapshot()`** (new GLGame method): players' stats overwrite; remote ship pose snaps; **local ship blends** (`lerp` factor ~0.35/snapshot); projectiles replaced from NetExtras. **Asteroids get a `uint32_t net_id`** (asteroid.h/cpp, host-assigned counter) — client keeps an id→Asteroid map: match → overwrite pose/health/transient flags (all already in `Save::Asteroid`); new → construct+`restore_state` (existing path); missing → play the existing death visual (debris to `dead_objects`, mirroring glgame.cpp:884–890) so kills still explode. Pickups rebuilt each snapshot; station/mini-station/enemies via existing `restore_state`. Generation change (world size differs) → replicate the rollover block (glgame.cpp:616–658): new Grid/boundaries/starfield, then apply.
- **Rendering online**: `num_x/y_viewports()` return 1; each machine draws one full view following its local player (host = `players->front()`, client = `players->back()`).

## GLGame gating (all keyed on new `net_mode_ != Off`)

- **Save protection**: `save_progress()` (called from destructor glgame.cpp:136, back_pressed, focus_lost, death path glgame.cpp:1186) and game-over `delete_save()` become no-ops online — otherwise online play clobbers the local solo save.
- `maybe_start_intro()` returns immediately online; host sends GENERATION_START and both sides show a 2 s banner (name table at glgame.cpp:452–471) instead of the Intro state.
- `toggle_pause()` sends PAUSE/RESUME; receiving one toggles without re-sending. `focus_lost()` does NOT auto-pause online (mutes only).
- `add_player2` key/controller paths disabled online; new `add_remote_player()` (clone of glgame.cpp:557 minus controller/key bindings) creates the remote GLCar. `apply_remote_input()` drives the public Ship setters (ship.h:55–61) + one-shot counters; respawn-tap mirrors glship.cpp:513–517 semantics (friend access via ship.h:185).
- Debug/host-only keys (friendly-fire G, skip-level N, time scale) ignored on the client.

## Menu / lobby UX

- `menu.cpp`: ONLINE row after NEW GAME when `net_available()` — extend `max_menu_items()` (menu.cpp:661), draw block (~230–256), `confirm_selection()` (menu.cpp:706) → `new NetLobby()`. Optionally behind `is_beta_feature_enabled()` until phase 7 lands.
- NetLobby: CHOOSE → HOST ("offer copied to clipboard — send to friend; press V to paste answer") / JOIN ("press V to paste offer" → "answer copied — send back"). Base64 SDP + 1-byte offer/answer/magic prefix (catches wrong-direction pastes). Native clipboard via SDL; web via `navigator.clipboard` inside the V-key gesture (async poll, "clipboard blocked" fallback). On connect: HELLO/WELCOME, host starts `GLGame(session, Host, ctrl)`, client waits for snapshot #1. `failed()` → message → back to CHOOSE. In-game disconnect/BYE → "CONNECTION LOST" → Menu.

## Explicit Milestone-1 scope cuts

Fresh game only (no online save/resume); Intro states suppressed online (banner instead); no rejoin; STUN-only (no TURN — strict-NAT pairs get a clear failure message after ~20 s); host settings win; keyboard/controller lobby only; some remote one-shot sounds missed at 10 Hz; local high score only; exactly 2 players.

## Implementation phases (each lands with all CI green)

0. **Spike**: prove FetchContent MbedTLS + libdatachannel `USE_MBEDTLS` builds /MT under MSVC Ninja (the riskiest external); confirm pin + submodule fetch + C-API reliability struct. Adjust pins.
1. **Seam**: all `net_*.h/cpp` as no-op stubs (factory → nullptr). Zero behavior change everywhere.
2. **Native backend**: CMake block + fill `net_transport_rtc.cpp`; hidden `NEWTONIA_NET_SELFTEST=1` in-process offer/answer loopback logging PASS/FAIL.
3. **Web backend**: fill `net_transport_web.cpp` (EM_JS + clipboard); test with two browser tabs.
4. **Savegame Stream refactor**: byte-identical verification.
5. **NetLobby + Menu + handshake**: two machines reach CONNECTED + HELLO/WELCOME.
6. **Host side**: net ctor, `add_remote_player`, `apply_remote_input`, snapshot capture/send, save/pause/intro gating, single viewport, asteroid `net_id`.
7. **Client side (the milestone)**: `tick_net_client`, `apply_snapshot`, net-id map, generation rebuild, local-ship blend, input send.
8. **Polish**: pause propagation, disconnect UX, generation banner, dead-man timeout, cross-play hardening.

## Verification

1. **Two `newtonia.exe` instances on one Windows machine** (ICE works on loopback/LAN): connect via copy-paste; both ships controllable; remote shoots/boosts/teleports/cycles weapons; host-side asteroid kill explodes on client; pickups reflect; pause from either side pauses both; level clear → banner → new world on both; kill one process → other shows CONNECTION LOST → Menu; **pre-existing solo save still loads afterward**.
2. **Native↔web cross-play**: `make web`, serve `web/dist`, browser joins native host and vice-versa; clipboard UX in Chrome + Firefox; 15 KB chunking against browser channels.
3. **CI**: xbox.yml/windows-glon12.yml build with the new FetchContent deps; console-smoke green by construction (no `NEWTONIA_NET_RTC` → empty TUs). Phase-2 loopback self-test is the automatable net check. Linux Xvfb workflow only regression-tests that the menu still works (no net backend there in M1).
4. Explicitly test tough/armoured/phasing/quantum levels online (snapshot apply must carry their transient state — fields verified present in `Save::Asteroid`).

## Key risks

- **MbedTLS/libdatachannel FetchContent interop** — de-risked by Phase 0 before any product code.
- **Snapshot bandwidth late-game** — measured in Phase 6; fallback: 5 Hz above a size threshold; delta snapshots = Milestone 2.
- **Web clipboard permissions** — reads only inside a key gesture; visible fallback message.
- **Solo-save corruption** — hard gate on `net_mode_`, covered in verification.

## Later milestones (out of scope, direction only)

M2: room codes + tiny hosted signaling (Cloudflare Worker or VPS — interface already in place), TURN fallback, delta snapshots. M3: Xbox/Android/iOS enablement (libdatachannel builds for all three; iOS xcodeproj needs manual file adds), touch lobby UX.
